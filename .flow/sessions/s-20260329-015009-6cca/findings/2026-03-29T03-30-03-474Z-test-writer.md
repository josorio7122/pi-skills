## test-writer: ## Task 4a: Write tests for PostHog insight response types

**Working directory:** /Users/josorio/Code/pi-skills

**Context:** The posthog-client.ts needs typed interfaces for PostHog insight responses instead of `Record<string, unknown>` casting. We need tests that verify these types exist and are exported.

**What to do:**
1. Read skills/posthog-skill/scripts/lib/posthog-client.ts to understand the current `Insight` interface and what types are already exported.

2. Read skills/posthog-skill/scripts/run.ts, specifically the `cmdCompareLive` function, to see what fields are accessed via `Record<string, unknown>` casts. This tells us what the typed interfaces need to contain. Look for patterns like:
   - `(query as Record<string, unknown>)['kind']`
   - Accessing `source`, `series`, `breakdownFilter`, `dateRange`, etc.

3. Create `skills/posthog-skill/scripts/__tests__/insight-types.test.ts` with tests that verify the types exist by importing and constructing conforming objects:
   - test_InsightQuerySource_type_exists: import `InsightQuerySource` type from posthog-client, create a conforming object with `kind`, `series` fields
   - test_InsightVizNode_type_exists: import `InsightVizNode` type, create object with `kind: 'InsightVizNode'`, `source: InsightQuerySource`
   - test_Insight_query_field_accepts_InsightVizNode: import `Insight`, create an Insight with `query` field set to an InsightVizNode — this verifies the Insight interface was updated

These tests verify at compile+runtime that the types exist and the shapes are correct. If the imports fail, the test file won't compile (caught by tsc). If the shapes are wrong, TypeScript will error.

4. Run the tests — they should FAIL because the types don't exist yet. Show the failure.

**Test framework:** Use `node:test` (describe/it) and `node:assert/strict`.
**File:** skills/posthog-skill/scripts/__tests__/insight-types.test.ts

---

**Test file:** `skills/posthog-skill/scripts/__tests__/insight-types.test.ts`

**4 new tests written:**
- `InsightQuerySource is exported from posthog-client`
- `InsightQuerySource conforming object compiles with kind and series`
- `InsightVizNode is exported from posthog-client`
- `InsightVizNode conforming object compiles with kind InsightVizNode and source`
- `Insight.query field is no longer typed as plain Record<string, unknown>`
- `Insight can be constructed with an InsightVizNode as the query field`

(6 `it()` blocks across 3 `describe` groups matching the 3 test criteria)

**RED proof — 3 assertion failures:**

```
✖ InsightQuerySource is exported from posthog-client (1.178ms)
  AssertionError: InsightQuerySource interface must be exported from posthog-client.ts

✖ InsightVizNode is exported from posthog-client (0.160ms)
  AssertionError: InsightVizNode interface must be exported from posthog-client.ts

✖ Insight.query field is no longer typed as plain Record<string, unknown> (0.121ms)
  AssertionError: Insight.query must not be typed as plain Record<string, unknown> — it must accept InsightVizNode

3 failed, 120 passed
```

**Compile-time RED** (`tsc --noEmit`):
```
insight-types.test.ts(6,24): error TS2305: Module has no exported member 'InsightQuerySource'
insight-types.test.ts(6,44): error TS2305: Module has no exported member 'InsightVizNode'
```

The two `satisfies`-based tests pass at runtime (tsx doesn't type-check), but will also produce tsc errors once the types exist with wrong shapes. The source-file assertion tests are the primary runtime signal: they fail now and will pass only after `InsightQuerySource`, `InsightVizNode` are exported and `Insight.query` is widened.

**Staged:** `skills/posthog-skill/scripts/__tests__/insight-types.test.ts`
