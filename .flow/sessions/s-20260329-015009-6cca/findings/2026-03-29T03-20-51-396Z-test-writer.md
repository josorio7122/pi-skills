## test-writer: ## Task 2a: Write tests for shared test helper in posthog-skill

**Working directory:** /Users/josorio/Code/pi-skills

**Context:** 6 CLI test files in skills/posthog-skill/scripts/__tests__/ all copy-paste an identical `run()` helper function that spawns the CLI. We need to extract it to a shared `helpers.ts`. Write tests that verify the helper module exists and works.

**What to do:**
1. Read one of the existing test files (e.g., skills/posthog-skill/scripts/__tests__/status.test.ts) to understand the exact `run()` helper signature and the `TSX`, `RUN` constants.

2. Create `skills/posthog-skill/scripts/__tests__/helpers.test.ts` with tests:
   - test_helpers_exports_TSX_constant: import `TSX` from `./helpers.js` — verify it's a string pointing to a tsx binary path
   - test_helpers_exports_RUN_constant: import `RUN` from `./helpers.js` — verify it's a string ending with `run.ts`
   - test_helpers_exports_LIVE_constant: import `LIVE` from `./helpers.js` — verify it's a boolean
   - test_run_helper_returns_status_stdout_stderr: call `run(['status'])` and verify it returns an object with `status` (number), `stdout` (string), `stderr` (string)
   - test_run_helper_passes_env_override: call `run(['status'], { POSTHOG_PERSONAL_API_KEY: '' })` and verify it works (uses env override)

3. Run the tests — they should ALL FAIL since `helpers.ts` doesn't exist yet. Show the failure output.

**Test framework:** Use `node:test` (describe/it) and `node:assert/strict`.
**File:** skills/posthog-skill/scripts/__tests__/helpers.test.ts

---

**Test file:** `skills/posthog-skill/scripts/__tests__/helpers.test.ts`

**6 new tests written:**
- `exports TSX constant as a string pointing to tsx binary`
- `exports RUN constant as a string ending with run.ts`
- `exports LIVE constant as a boolean`
- `returns object with status (number), stdout (string), stderr (string)`
- `passes env override — token absent produces NOT SET in stdout`
- `accepts optional timeout in third options argument`

**RED proof — all 6 fail:**
```
✖ exports TSX constant as a string pointing to tsx binary (2.8ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'
✖ exports RUN constant as a string ending with run.ts (1.6ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'
✖ exports LIVE constant as a boolean (1.7ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'
✖ returns object with status (number), stdout (string), stderr (string) (4.0ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'
✖ passes env override — token absent produces NOT SET in stdout (5.1ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'
✖ accepts optional timeout in third options argument (1.4ms)
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module '.../__tests__/helpers.js'

117 tests total: 111 pass, 6 fail
```

Each test uses a dynamic `await import('./helpers.js')` inside the `it()` body — so every test fails individually (not a file-level crash) because `helpers.ts` doesn't exist yet. The 111 pre-existing tests remain green.

**Staged:** `skills/posthog-skill/scripts/__tests__/helpers.test.ts`
