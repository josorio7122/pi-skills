## builder: ## Fix render-deploy — 6 issues

**Working directory:** /Users/josorio/Code/pi-skills

Read each file before editing.

### Issue 1 (HIGH): `sandbox_permissions` is pi-specific
**File: `skills/render-deploy/SKILL.md`**
- Find ALL occurrences of `sandbox_permissions=require_escalated` or `sandbox_permissions`
- Replace with environment-agnostic language:
```markdown
If deployment fails due to network timeouts or DNS errors, the execution environment may be blocking outbound connections. Ensure the environment allows outbound HTTPS to `api.render.com` and `dashboard.render.com` before retrying.
```
- In the Troubleshooting section, replace the escalated permissions example with:
```markdown
The deploy needs outbound network access to reach Render's API. If your environment restricts network calls, grant access to `api.render.com` and `dashboard.render.com`, then retry.
```

### Issue 2 (MEDIUM): Two conflicting Prerequisites sections
**File: `skills/render-deploy/SKILL.md`**
- Find the first `## Prerequisites` section (the short 2-line stub near the top)
- Merge its unique content into the `## Prerequisites Check` section
- Delete the stub `## Prerequisites` section

### Issue 3 (MEDIUM): Description missing triggers
**File: `skills/render-deploy/SKILL.md`**
- In the frontmatter `description`, append database/cron triggers:
- Change: `"...or set up their application on Render's cloud platform."`
- To: `"...or set up their application on Render's cloud platform, including databases, cron jobs, and background workers."`

### Issue 4 (MEDIUM): "When to Use This Skill" section redundant
- Find and delete the `## When to Use This Skill` section — its content is now covered by the enhanced description

### Issue 5 (MEDIUM): `curl | sh` before warning
**File: `skills/render-deploy/SKILL.md`**
- In the Prerequisites Check, find the `curl | sh` install command
- Move the `⚠️ Security` warning ABOVE the command, not below it

### Issue 6 (MEDIUM): Time-sensitive pricing
**File: `skills/render-deploy/references/blueprint-spec.md`** and **`references/configuration-guide.md`**
- Find any hardcoded prices (e.g., `$7/month`, `$25/month`, `750 hours/month`)
- Add a note at the top of any pricing table: "Pricing may change. Verify at https://render.com/pricing."
- Keep the table for reference but add the caveat

### Issue 7 (LOW): PostgreSQL version 15 in asset templates
**Files: `skills/render-deploy/assets/*.yaml`**
- Find all `postgresMajorVersion: '15'` and update to `postgresMajorVersion: '16'`
- Add a YAML comment: `# Update to latest — see https://render.com/docs/postgresql-creating-connecting`

### Issue 8 (LOW): Node.js 21 in supported versions
**Files: `skills/render-deploy/references/blueprint-spec.md`** and **`references/runtimes.md`**
- Find the Node.js version lists that include `21`
- Remove `21`, add `22` if not present

### Issue 9: Remove `compatibility` frontmatter field

### Issue 10 (INFO): "This skill covers" descriptive voice
- Find `This skill covers` at the top of SKILL.md body and change to imperative

### Verify
```bash
grep -rn 'sandbox_permissions' skills/render-deploy/
grep -n 'When to Use This Skill' skills/render-deploy/SKILL.md
grep -rn "postgresMajorVersion: '15'" skills/render-deploy/
grep -n 'compatibility' skills/render-deploy/SKILL.md
pnpm run format -- skills/render-deploy/
pnpm run format:check
```

**Boundaries:** Only modify render-deploy files.

All changes are exactly as intended. Here's the summary:

---

## Changes made

**`skills/render-deploy/SKILL.md`**
- **Issue 1**: Replaced `sandbox_permissions=require_escalated` in Troubleshooting with env-agnostic network access guidance; renamed section `### Network Access`
- **Issue 2**: Deleted stub `## Prerequisites` section; merged the timeout note into `## Prerequisites Check` as a `> Note:` callout
- **Issue 3**: Appended `", including databases, cron jobs, and background workers."` to frontmatter description
- **Issue 4**: Deleted `## When to Use This Skill` section entirely
- **Issue 5**: Moved `⚠️ Security` blockquote above the `curl | sh` install line
- **Issue 9**: Removed `compatibility:` frontmatter field
- **Issue 10**: Changed `"This skill covers **Git-backed** flows:"` → `"Use one of these **Git-backed** deployment flows:"`

**`skills/render-deploy/references/blueprint-spec.md`**
- **Issue 6**: Added `> Note: Pricing may change. Verify at https://render.com/pricing.` above the Service Plans table
- **Issue 7**: Updated `postgresMajorVersion: '15'` → `'16'` with doc comment (×2 in complete example)
- **Issue 8**: Changed Node.js version list `14, 16, 18, 20, 21` → `14, 16, 18, 20, 22`

**`skills/render-deploy/references/configuration-guide.md`**
- **Issue 6**: Added pricing caveat note above the Free Tier "What's Included" list

**`skills/render-deploy/references/runtimes.md`**
- **Issue 8**: Changed Node.js supported versions `14, 16, 18, 20, 21` → `14, 16, 18, 20, 22`

**`skills/render-deploy/assets/docker.yaml`**, **`go-api.yaml`**, **`nextjs-postgres.yaml`**, **`python-django.yaml`**
- **Issue 7**: Updated `postgresMajorVersion: '15'` → `'16'` with `# Update to latest — see https://render.com/docs/postgresql-creating-connecting` comment in all four templates

**Verification:** `pnpm run format:check` → `All matched files use Prettier code style!` ✓
