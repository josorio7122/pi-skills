## test-writer: ## Task 2a: Verify pre-conditions for test centralization

**Working directory:** /Users/josorio/Code/pi-skills

**What to do:**

1. **Confirm the local TSX path pattern exists in test files.** Search all files in `skills/posthog-skill/scripts/__tests__/` for the pattern that resolves tsx from LOCAL node_modules: `path.join(__dirname, '..', '..', 'node_modules', '.bin', 'tsx')` (2 levels up from __tests__ to posthog-skill root). List every file that contains this pattern.

2. **Confirm `pnpm test` fails at root.** Run `pnpm test` from /Users/josorio/Code/pi-skills — it should fail because there's no `test` script in root package.json yet. Show the error output.

3. **Confirm the files to be deleted exist:**
   - `skills/posthog-skill/package.json` exists
   - `skills/posthog-skill/tsconfig.json` exists  
   - `skills/posthog-skill/pnpm-lock.yaml` exists

**Output format:** For each check, show the command run and the output. Clearly mark each as PASS (pre-condition met) or FAIL (unexpected).

All three checks complete. Here are the results:

---

## Check 1 — Local TSX path pattern in test files

**Command:** `grep -rl "path.join(__dirname, '..', '..', 'node_modules', '.bin', 'tsx')" skills/posthog-skill/scripts/__tests__/`

**Output:**
```
skills/posthog-skill/scripts/__tests__/flags.test.ts
skills/posthog-skill/scripts/__tests__/compare.test.ts
skills/posthog-skill/scripts/__tests__/status.test.ts
skills/posthog-skill/scripts/__tests__/create.test.ts
skills/posthog-skill/scripts/__tests__/inspect.test.ts
skills/posthog-skill/scripts/__tests__/live.test.ts
```

**✅ PASS** — All 6 test files contain the local (2-levels-up) tsx path pattern. Every one of them will need updating when centralized.

---

## Check 2 — `pnpm test` fails at root

**Command:** `pnpm run test` from `/Users/josorio/Code/pi-skills`

**Output:**
```
ERR_PNPM_NO_SCRIPT  Missing script: test

Command "test" not found.
Exit code: 1
```

Root `package.json` has `"scripts": {}` — no `test` script defined.

**✅ PASS** — Root `pnpm test` correctly fails. Adding a `test` script is a pre-condition for Task 2b.

---

## Check 3 — Files to be deleted exist

**Command:** `[ -f "$f" ]` for each path

**Output:**
```
EXISTS: skills/posthog-skill/package.json
EXISTS: skills/posthog-skill/tsconfig.json
EXISTS: skills/posthog-skill/pnpm-lock.yaml
```

**✅ PASS** — All three files slated for deletion are present.

---

## Summary

| Check | Result |
|---|---|
| Local tsx pattern in 6 test files | ✅ PASS |
| `pnpm test` fails at root (no script) | ✅ PASS |
| 3 files-to-delete exist | ✅ PASS |

All pre-conditions are met. Task 2b (the actual centralization) can proceed.
