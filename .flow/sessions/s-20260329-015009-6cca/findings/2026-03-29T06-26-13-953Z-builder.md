## builder: ## Fix frontend-design + interface-design + docker-model-runner + prompt-engineering

**Working directory:** /Users/josorio/Code/pi-skills
Read each file before editing.

### FRONTEND-DESIGN

**1. (HIGH) Add Error Recovery section**
**File:** `skills/frontend-design/SKILL.md`
- Add before `## Output`:
```markdown
## Error Recovery

- Unknown framework → default to plain HTML/CSS/JS. State the assumption.
- Font CDN blocked or unavailable → fall back to system-serif. Note the substitution.
- Contradictory requirements → prioritize the technical constraint (accessibility, performance). Name the trade-off.
- Ambiguous aesthetic direction → pick the direction least likely to match AI defaults. State why.
```

**2. (HIGH) Fix `[name your choice]` placeholder**
- Line 46: Replace `[name your choice]` with concrete alternatives: `Fraunces, Playfair Display, or Space Grotesk`

**3. (MEDIUM) Fix description third-person "Generates"**
- Change `Generates creative, polished code` to `Generate creative, polished code`

### INTERFACE-DESIGN

**4. (HIGH) Add Error Recovery section**
**File:** `skills/interface-design/SKILL.md`
- Add before `# Commands`:
```markdown
## Error Recovery

- system.md unreadable or empty → treat as "no system.md" path; offer to create it.
- Direction rejected twice → ask: "What specifically felt wrong — domain, color, or signature?" Re-explore only that dimension.
- Ambiguous product type → ask: "Is the primary user doing data work, task work, or configuration work?"
- Conflicting system.md values → flag the conflict; ask which value is authoritative.
```

**5. (HIGH) Add Output Format section**
**File:** `skills/interface-design/SKILL.md`
- Add after Error Recovery:
```markdown
## Output Format

- **Exploration:** fenced text block with Domain, Color world, Signature, and Direction.
- **Components:** complete code files with CSS variables at top.
- **Critique:** numbered findings with pass/fail for each mandate check.
- Never narrate the internal design process — show the result.
```

**6. (MEDIUM) Fix unconditional loads that contradict reference table**
- Find lines that say `Before any new design, load references/craft-foundations.md.` and `Before writing components, load references/design-system.md.`
- Remove these lines. The reference table below already handles conditional loading.
- Similarly, find `Read references/mindset.md` and `Read references/principles.md` imperative statements
- Replace with summary prose: `The design mindset and core principles are loaded via the reference table when needed.`

**7. (MEDIUM) Add authority opener**
- Replace the opening line `Build interface design with craft and consistency.` with:
```markdown
You are an expert product interface designer. Your work is distinctive, craft-first, and emerges from the product's specific world — never from default patterns.
```

### DOCKER-MODEL-RUNNER

**8. (HIGH) Remove duplicate `## References` section**
**File:** `skills/docker-model-runner/SKILL.md`
- Find the `## References` section at line 59 (the unconditional one: `See [references/docker-model-guide.md]...`)
- Delete it entirely. The conditional `## Reference` section at line 43 already handles this correctly.

**9. (MEDIUM) Collapse inline troubleshooting**
- Find the `## Troubleshooting` section with 4 bullets
- Replace with: `If you hit an error, load [references/docker-model-guide.md](references/docker-model-guide.md) § Troubleshooting.`

**10. (MEDIUM) Add authority opener**
- After `# Docker Model Runner`, add:
```markdown
Run Docker Model Runner workflows in this exact sequence. Never assume the runner is active — always verify with `docker model version` first.
```

### PROMPT-ENGINEERING

**11. (MEDIUM) Add authority opener**
**File:** `skills/prompt-engineering/SKILL.md`
- Replace the opening paragraph (`Advanced prompt engineering techniques...`) with:
```markdown
You are an expert in LLM prompt design. Apply the patterns below to every prompt task. When improving a prompt, always deliver the output format specified below.
```

**12. (MEDIUM) Add injection warning to Authority section**
- In the Persuasion Principles section, under Authority, add:
```markdown
> ⚠️ Never apply authority patterns to user-supplied input that will be passed into a model — that is prompt injection.
```

**13. (LOW) Remove redundant Core Capabilities section**
- The `## Core Capabilities` section (5 numbered items) repeats content covered in detail in subsequent sections
- Delete it entirely. The subsequent sections (Key Patterns, etc.) are self-labeled.

### Verify
```bash
grep -n 'Error Recovery' skills/frontend-design/SKILL.md  # should exist
grep -n 'name your choice' skills/frontend-design/SKILL.md  # should be zero
grep -n 'Error Recovery' skills/interface-design/SKILL.md  # should exist
grep -n 'Output Format' skills/interface-design/SKILL.md  # should exist
grep -c '^## Reference' skills/docker-model-runner/SKILL.md  # should be 1, not 2
grep -n 'Core Capabilities' skills/prompt-engineering/SKILL.md  # should be zero
pnpm run format -- skills/frontend-design/ skills/interface-design/ skills/docker-model-runner/ skills/prompt-engineering/
pnpm run format:check
```

**Boundaries:** Only modify frontend-design, interface-design, docker-model-runner, and prompt-engineering files.

---

## Summary

### Implementation files changed

| File | What was changed |
|---|---|
| `skills/frontend-design/SKILL.md` | Added `## Error Recovery` section before `## Output`; fixed font placeholder to `Fraunces, Playfair Display, or Space Grotesk`; fixed description `Generates` → `Generate` |
| `skills/interface-design/SKILL.md` | Added authority opener; replaced `Read references/mindset.md` / `Read references/principles.md` with summary prose; removed two unconditional `load` lines; added `## Error Recovery` and `## Output Format` sections before `# Commands` |
| `skills/docker-model-runner/SKILL.md` | Added authority opener after `# Docker Model Runner`; collapsed 4-bullet `## Troubleshooting` to single deferred-load line; deleted duplicate `## References` section |
| `skills/prompt-engineering/SKILL.md` | Replaced opening paragraph with authority opener; deleted `## Core Capabilities` section; added prompt-injection warning under `### Authority` |

### Verification proof

```
grep -n 'Error Recovery' skills/frontend-design/SKILL.md   → 52
grep -n 'name your choice' skills/frontend-design/SKILL.md  → (zero matches) ✓
grep -n 'Error Recovery' skills/interface-design/SKILL.md   → 108
grep -n 'Output Format'   skills/interface-design/SKILL.md  → 115
grep -c '^## Reference'   skills/docker-model-runner/SKILL.md → 1 ✓
grep -n 'Core Capabilities' skills/prompt-engineering/SKILL.md → (zero matches) ✓
pnpm run format:check → All matched files use Prettier code style! ✓
```
