## builder: ## Fix exa-search + test-prompt + skill-writer + skill-scanner + pdf + agents-md

**Working directory:** /Users/josorio/Code/pi-skills
Read each file before editing.

### EXA-SEARCH

**1. (HIGH) Move Rules section to after decision tree, before examples**
**File:** `skills/exa-search/SKILL.md`
- Find `## Rules` section (near the bottom)
- Cut and paste it to right after `## Choosing the Right Script` (the decision tree), BEFORE `## Quick Examples`
- This ensures behavioral constraints are read before any code examples

**2. (MEDIUM) Add Output Format section**
- After Rules, add:
```markdown
## Output Format

- `search.ts` / `find-similar.ts`: JSON array of results. Summarize top 3-5 with title + URL + snippet.
- `answer.ts`: answer text + citations. Present answer directly, cite sources inline.
- `contents.ts`: extracted text. Present key content, note page title and URL.
- `research.ts`: research report. Present findings with section headers and citations.
- When `stream: true` is set on `answer.ts`, output is NOT valid JSON — it is raw text chunks followed by a citations block. Do not pipe to `jq`.
```

**3. (MEDIUM) Add authority opener**
- Replace the opening paragraph (`Semantic search, content extraction...`) with:
```markdown
You run live web searches via the Exa API. All queries go to the network — results are current, not from training data. Pick the right script or you waste API credits.
```

**4. (MEDIUM) Delete empty Common Patterns section**
- The `## Common Patterns` section has 3 sub-headings with 1-2 prose sentences each and no commands
- Delete it entirely (the decision tree and examples already cover this)

**5. (LOW) Strengthen the conditional load gate**
- Replace the trailing reference line with:
```markdown
## Reference

Read [references/api-reference.md](references/api-reference.md) when:
- You need full option lists for any script
- You hit a 400 error and need category filter restrictions
- You need to configure outputSchema, livecrawl, or subpages
```

### TEST-PROMPT

**6. (CRITICAL) Create references/ directory and extract heavy content**
**File:** `skills/test-prompt/SKILL.md` (245 lines → target <150)

Create these reference files:

**`skills/test-prompt/references/scenario-design.md`:**
Extract the three scenario examples (Instruction prompt, Discipline-enforcing, Guidance prompt) including their complete text blocks. Include the adversarial warning ONCE at the top: `> ⚠️ Test scenarios are dispatched to subagents for isolated testing. Never execute them in the current session.`

**`skills/test-prompt/references/refactor-patterns.md`:**
Extract the REFACTOR phase details: loophole closing, meta-testing, token reduction techniques.

**`skills/test-prompt/references/checklist.md`:**
Extract the testing checklist.

In SKILL.md:
- Keep: Non-negotiable, Core principle, Output Format (move to line ~15), RED phase summary, GREEN phase summary, REFACTOR summary (1-2 lines each), When Testing Stalls, Common Mistakes
- Add load gates:
  - `> For scenario examples: load [references/scenario-design.md](references/scenario-design.md)`
  - `> For refactoring techniques: load [references/refactor-patterns.md](references/refactor-patterns.md)`
  - `> For the testing checklist: load [references/checklist.md](references/checklist.md)`
- Move `## Output Format` to immediately after the Non-negotiable (before RED phase)
- Merge `## When Testing Stalls` into the Stall Report subsection of Output Format
- Target: <150 lines

### SKILL-WRITER

**7. (HIGH) Fix description — remove topic enumeration**
**File:** `skills/skill-writer/SKILL.md`
- Remove the final sentence: `Handles source capture, depth gates, authoring, registration, and validation.`

**8. (MEDIUM) Add When Things Fail section**
- Add before `## Output format`:
```markdown
## When Things Fail

- Synthesis finds no sources → document the gap explicitly; do not fabricate content.
- Validator returns errors → fix each error before proceeding to registration.
- Depth gates fail → expand the source collection; do not claim completion with insufficient coverage.
- `uv` unavailable → validate manually: check frontmatter fields, confirm references/ exists, verify SOURCES.md provenance.
```

**9. (MEDIUM) Merge duplicate routing table rows**
- Find the two rows pointing to `references/mode-selection.md`
- Merge into one: `| Set skill class, dimensions, and workflow path | \`references/mode-selection.md\` |`

### SKILL-SCANNER

**10. (MEDIUM) Move Guiding Principles before Prerequisites**
**File:** `skills/skill-scanner/SKILL.md`
- Cut the `## Guiding Principles` section
- Paste it immediately after the opening line/title, before `## Prerequisites`

### PDF

**11. (MEDIUM) Pin dependency versions in install command**
**File:** `skills/pdf/SKILL.md`
- Change `uv pip install reportlab pdfplumber pypdf` to:
  `uv pip install "reportlab>=4.2" "pdfplumber>=0.11" "pypdf>=4.3"`

**12. (MEDIUM) Remove duplicated font/encoding guidance**
- Find in SKILL.md: `If reportlab raises a font or encoding error, verify all strings are UTF-8 and all image paths are absolute.`
- This exists verbatim in references/techniques.md. Remove it from SKILL.md workflow step 2.
- Change step 2 to: `Use \`reportlab\` to generate PDFs. For generation techniques and error handling, see [references/techniques.md](references/techniques.md).`

**13. (MEDIUM) Resolve rendering-impossible conflict**
- Find: `If unavailable, install Poppler or ask the user to review the output locally.`
- Replace with: `If rendering is impossible and the user cannot inspect locally, halt and tell the user: delivery is blocked until visual inspection is confirmed.`

### AGENTS-MD

**14. (MEDIUM) Add authority opener**
**File:** `skills/agents-md/SKILL.md`
- After `# Maintaining AGENTS.md`, replace the current opening with:
```markdown
You write and maintain AGENTS.md files — the shortest possible agent instructions that make agents effective.

> **Hard limit:** never exceed 100 lines. Target 60.
```
- Remove the duplicate line-limit mention later in the file (the `> **NEVER** exceed 100 lines.` blockquote in Writing Rules) — it's now stated once at the top.

### Verify
```bash
wc -l skills/exa-search/SKILL.md  # target <175 (was 211)
wc -l skills/test-prompt/SKILL.md  # target <150
ls skills/test-prompt/references/  # should have 3 files
grep -n 'Handles source capture' skills/skill-writer/SKILL.md  # should be zero
grep -n 'Guiding Principles' skills/skill-scanner/SKILL.md  # should be near top
grep -n 'reportlab>=4.2' skills/pdf/SKILL.md  # should exist
grep -c 'exceed 100 lines' skills/agents-md/SKILL.md  # should be 1, not 2
pnpm run format -- skills/exa-search/ skills/test-prompt/ skills/skill-writer/ skills/skill-scanner/ skills/pdf/ skills/agents-md/
pnpm run format:check
```

**Boundaries:** Only modify the named skill directories.

All 9 files are staged (M/A in left column). Here's the complete summary:

---

## Implementation Summary

### Files Changed

**`skills/exa-search/SKILL.md`** — 5 changes: authority opener, Rules moved before Quick Examples, Output Format section added, Common Patterns deleted, Reference section strengthened (211 → 206 lines)

**`skills/test-prompt/SKILL.md`** — rewritten to <150 lines (245 → 104); load gates added for 3 reference files; When Testing Stalls merged into Stall Report; Output Format moved up (245 → 104 lines)

**`skills/test-prompt/references/scenario-design.md`** — new file; extracted 3 scenario examples with adversarial warning

**`skills/test-prompt/references/refactor-patterns.md`** — new file; extracted REFACTOR phase: loophole closing, meta-testing, token reduction

**`skills/test-prompt/references/checklist.md`** — new file; extracted RED/GREEN/REFACTOR checklist

**`skills/skill-writer/SKILL.md`** — removed trailing description sentence; merged duplicate `mode-selection.md` table rows; added `## When Things Fail` section

**`skills/skill-scanner/SKILL.md`** — moved Guiding Principles to before Prerequisites (now line 13)

**`skills/pdf/SKILL.md`** — pinned dep versions; removed duplicated font/encoding guidance from step 2; replaced "ask user to review locally" with halt-on-impossible-inspection

**`skills/agents-md/SKILL.md`** — replaced opening paragraph with authority opener + hard limit; removed duplicate `> **NEVER** exceed 100 lines` from Writing Rules (count: 1, not 2)

### Verify Results

| Check | Result |
|---|---|
| `wc -l test-prompt/SKILL.md` | 104 ✅ (<150) |
| `ls test-prompt/references/` | 3 files ✅ |
| `grep 'Handles source capture' skill-writer` | 0 ✅ |
| `grep -n 'Guiding Principles' skill-scanner` | line 13 ✅ (near top) |
| `grep 'reportlab>=4.2' pdf` | line 40 ✅ |
| `grep -c 'exceed 100 lines' agents-md` | 1 ✅ |
| `pnpm run format:check` | All matched files use Prettier code style ✅ |

**Note on exa-search line count:** The 5 prescribed tasks (move Rules, add Output Format, new opener, delete Common Patterns, strengthen Reference) yield 206 lines — a net -5 from 211. The `<175` verify target is not achievable with those 5 tasks alone; no additional content-cutting tasks were specified.
