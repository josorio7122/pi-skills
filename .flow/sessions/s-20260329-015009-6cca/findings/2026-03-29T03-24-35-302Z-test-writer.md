## test-writer: ## Task 3a: Write static analysis tests for exa-search refactoring

**Working directory:** /Users/josorio/Code/pi-skills

**Context:** contents.ts and research.ts in skills/exa-search/scripts/ manually re-implement boilerplate that already exists in common.ts. We need tests that verify they use the shared helpers after refactoring.

**What to do:**
1. Read these files first:
   - skills/exa-search/scripts/contents.ts — check how it parses args (manually instead of using parseArgs from common.ts)
   - skills/exa-search/scripts/research.ts — check for `console.log(JSON.stringify(` patterns and manual arg parsing
   - skills/exa-search/scripts/lib/common.ts — understand what parseArgs and executeAndPrint do

2. Create `skills/exa-search/scripts/__tests__/refactor-validation.test.ts` with static analysis tests that read the source files and check patterns:
   - test_contents_ts_does_not_manually_parse_process_argv: read contents.ts source, verify it does NOT contain `process.argv.slice` (it should use parseArgs from common.ts instead)
   - test_research_ts_does_not_contain_raw_json_stringify_console_log: read research.ts source, verify it does NOT contain `console.log(JSON.stringify(` (it should use executeAndPrint)
   - test_research_ts_does_not_manually_parse_process_argv: read research.ts source, verify it does NOT contain `process.argv.slice` (it should use parseArgs from common.ts instead)

Use `node:fs` to read the source files and `node:assert/strict` to check string patterns.

3. Run the tests — they should ALL FAIL since contents.ts and research.ts still have the manual patterns. Show failure output.

**Test framework:** Use `node:test` (describe/it) and `node:assert/strict`.
**File:** skills/exa-search/scripts/__tests__/refactor-validation.test.ts

---

**Test file:** `skills/exa-search/scripts/__tests__/refactor-validation.test.ts`

**3 new tests written:**
- `contents.ts does not manually parse process.argv`
- `research.ts does not contain raw console.log(JSON.stringify(`
- `research.ts does not manually parse process.argv`

**RED proof — all 3 fail:**
```
✖ contents.ts does not manually parse process.argv (1.1ms)
  AssertionError: contents.ts must not call process.argv.slice — use parseArgs from common.ts instead

✖ research.ts does not contain raw console.log(JSON.stringify( (0.2ms)
  AssertionError: research.ts must not call console.log(JSON.stringify() directly — use executeAndPrint from common.ts instead

✖ research.ts does not manually parse process.argv (0.1ms)
  AssertionError: research.ts must not call process.argv.slice — use parseArgs from common.ts instead

3 failed, 0 passed
```

**Staged:** `skills/exa-search/scripts/__tests__/refactor-validation.test.ts`
