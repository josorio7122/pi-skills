## builder: ## Task 5b: Split run.ts into command modules

**Working directory:** /Users/josorio/Code/pi-skills

**This is the most complex task. Read everything carefully before making changes.**

### Step 1: Read ALL files
- skills/posthog-skill/scripts/run.ts — the 502-line god file to split
- skills/posthog-skill/scripts/lib/posthog-client.ts — for import types (PostHogConfig, PostHogError, createClient, InsightVizNode, InsightQuerySource)
- skills/posthog-skill/scripts/lib/dashboard-spec.ts — imported by run.ts as `* as spec`
- skills/posthog-skill/scripts/lib/fixtures.ts — imported for getFixture
- skills/posthog-skill/scripts/__tests__/cmd-modules.test.ts — understand what tests expect

### Step 2: Create lib/config.ts
Extract from run.ts:
- `AppConfig` interface (extends PostHogConfig with `isDryRun`, `host`, `projectId`, `personalApiKey`)
- `resolveConfig()` function
- `requireToken(config)` function
- `out(data)` function (JSON stringify to stdout)
- `info(msg)` function (write to stderr)
- `handleApiError(err, context)` function

Export ALL of these. Import PostHogConfig, PostHogError from posthog-client.

### Step 3: Create command modules
Each file exports a single main function. Import config helpers from `./config.js`.

**lib/cmd-status.ts:**
- Export `cmdStatus(config: AppConfig): void`
- Simple sync function, no API calls

**lib/cmd-inspect.ts:**
- Export `cmdInspect(flags: string[], config: AppConfig): Promise<void>`
- Contains both dry-run and live paths (cmdInspectLive inlined or as private function)
- Imports: config helpers, getFixture, dashboard-spec, createClient

**lib/cmd-compare.ts:**
- Export `cmdCompare(flags: string[], config: AppConfig): Promise<void>`
- Contains both dry-run and live paths
- **Use the new InsightVizNode/InsightQuerySource types** instead of Record<string, unknown> casts
- Imports: config helpers, getFixture, createClient, InsightVizNode, InsightQuerySource

**lib/cmd-create.ts:**
- Export `cmdCreate(flags: string[], config: AppConfig): Promise<void>`
- Contains both dry-run and live paths (cmdCreateLive is the big one ~100 lines)
- Imports: config helpers, getFixture, dashboard-spec, createClient

**lib/cmd-flags.ts:**
- Export `cmdFlags(args: string[], config: AppConfig): Promise<void>`
- Contains `parseFlagsOptions()` as a private helper and the main `cmdFlags` function with its switch
- Imports: config helpers, getFixture, createClient

### Step 4: Slim down run.ts
After extraction, run.ts should only contain:
- Imports from the new modules
- `main()` function with the command switch
- `.catch()` error handler
- A help/usage function (optional, inline is fine)

Target: **under 80 lines**.

### Step 5: Verify
- `pnpm test` — ALL 134 tests pass (existing CLI tests + new structural tests)
- `pnpm exec tsc --noEmit` — clean
- `pnpm run lint` — clean

**Critical:** The existing CLI tests (status, inspect, compare, create, flags, live) test via subprocess by spawning run.ts. They must continue to pass unchanged. The internal restructuring is invisible to them as long as run.ts still works as an entry point.

**Boundaries:** Create the new lib/*.ts files and rewrite run.ts. Do not modify any test files or other existing lib files (except using the new types from posthog-client.ts in cmd-compare.ts).

All staged. 

---

**Task: Split run.ts into command modules**

**Implementation files created/modified:**
- `skills/posthog-skill/scripts/lib/config.ts` — new: `AppConfig` interface, `resolveConfig`, `requireToken`, `out`, `info`, `handleApiError`
- `skills/posthog-skill/scripts/lib/cmd-status.ts` — new: extracted `cmdStatus`
- `skills/posthog-skill/scripts/lib/cmd-inspect.ts` — new: extracted `cmdInspect` + `cmdInspectLive`
- `skills/posthog-skill/scripts/lib/cmd-compare.ts` — new: extracted `cmdCompare` + `cmdCompareLive` (uses typed `InsightVizNode`/`InsightQuerySource`)
- `skills/posthog-skill/scripts/lib/cmd-create.ts` — new: extracted `cmdCreate` + `cmdCreateLive`
- `skills/posthog-skill/scripts/lib/cmd-flags.ts` — new: extracted `parseFlagsOptions` + `cmdFlags`
- `skills/posthog-skill/scripts/run.ts` — slimmed from 502 → 63 lines (imports + `cmdHelp` + `main` switch)

**GREEN proof:**
```
ℹ tests 134
ℹ suites 57
ℹ pass 134
ℹ fail 0
ℹ duration_ms 7022.929958
```
- `pnpm exec tsc --noEmit` → TSC CLEAN
- `pnpm run lint` → clean
