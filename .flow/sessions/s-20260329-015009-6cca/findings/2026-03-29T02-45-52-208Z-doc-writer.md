## doc-writer: ## Task D1: Update posthog-skill SKILL.md install instructions

**Working directory:** /Users/josorio/Code/pi-skills

**File to update:** skills/posthog-skill/SKILL.md

**What changed:** The posthog-skill no longer has its own package.json, tsconfig.json, or node_modules. All dependencies (tsx, typescript, @types/node) are managed at the repo root. Tests run from root via `pnpm test`.

**Changes needed:**

1. In the frontmatter `compatibility` field: change text about "Run pnpm install in the skill directory on first use" to say dependencies are managed at the repository root.

2. In the body text (around line 21 or wherever it says "Run `pnpm install` in the skill directory on first use to install `tsx` and type dependencies"): update to say "Dependencies (`tsx`, `typescript`, `@types/node`) are installed at the repository root. Run `pnpm install` from the repo root if needed."

3. Verify that any `npx tsx --test` commands in the file still work — these use `npx` which resolves up the node_modules tree, so they should be fine. Don't change those.

**Boundaries:** Only update install/setup instructions. Do not change command examples, test patterns, or any other content.

---

**File written:** `skills/posthog-skill/SKILL.md`

**Sections updated:**
1. **Frontmatter `compatibility`** — replaced "Run `pnpm install` in the skill directory on first use" with "Dependencies are managed at the repository root — run `pnpm install` from the repo root if needed."
2. **Body line 17** — replaced "Run `pnpm install` in the skill directory on first use to install `tsx` and type dependencies" with "Dependencies (`tsx`, `typescript`, `@types/node`) are installed at the repository root. Run `pnpm install` from the repo root if needed."
3. **"First Use" Step 0** — replaced `cd $SKILL_DIR && pnpm install` with a comment + bare `pnpm install` to be run from the repo root.

**Verification:**
- ✅ No remaining references to "skill directory" for installation
- ✅ All `npx tsx` command examples are untouched — `npx` resolves up the `node_modules` tree from the repo root, so they continue to work
- ✅ No command examples, test patterns, or other content modified

**Staged:** `skills/posthog-skill/SKILL.md`
