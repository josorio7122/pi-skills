## builder: ## Fix skill-writer + skill-scanner + playwright — mixed fixes

**Working directory:** /Users/josorio/Code/pi-skills
Read each file before editing.

### skill-writer fixes

**1. (HIGH) Create actual transformed skill artifacts**
The skill's own hard-stop rule requires happy-path, robust, and anti-pattern+fix example artifacts. Currently only case-study narratives exist.

Create `skills/skill-writer/references/examples/happy-path-skill.md`:
```markdown
# Example: Happy-Path Skill Output

This is what a correctly authored SKILL.md looks like.

\`\`\`yaml
---
name: example-tool
description: Automate example-tool tasks from the command line. Use when the user says "run example", "check example status", or any example-tool-related task.
---
\`\`\`

\`\`\`markdown
# Example Tool

> **Core rule:** Always verify connection before running commands.

## Workflow

1. Check prerequisites: \`example-tool --version\`
2. Run the requested operation
3. Report results with the command run and output summary

## Reference

Load [references/commands.md](references/commands.md) when the user needs a command not shown above.

## Output

After each command, report: command run, result summary, any warnings.

## When Things Fail

- **Connection refused** — verify the service is running
- **Auth error** — run \`example-tool auth refresh\`
\`\`\`
```

Create `skills/skill-writer/references/examples/antipattern-skill.md`:
```markdown
# Example: Anti-Pattern Skill + Correction

## ❌ Anti-Pattern SKILL.md

\`\`\`yaml
---
name: my-tool
description: This skill provides comprehensive functionality for my-tool including installation, configuration, troubleshooting, advanced usage patterns, and integration with other tools. It covers many topics.
metadata:
  author: someone
  version: '1.0'
---
\`\`\`

Problems:
- Description is descriptive, not trigger-based
- Topic enumeration ("including installation, configuration...")
- Non-standard frontmatter (metadata)
- No imperative triggers ("Use when...")

## ✅ Corrected Version

\`\`\`yaml
---
name: my-tool
description: Automate my-tool from the command line. Use when asked to "configure my-tool", "troubleshoot my-tool", or any my-tool-related task.
---
\`\`\`
```

**2. (MEDIUM) Fix provider-specific URL in synthesis-path.md**
**File:** `skills/skill-writer/references/synthesis-path.md`
- Find: `Anthropic/Claude upstream skill-authoring source (for example \`anthropics/skills/.../skill-creator\``
- Replace with: `Upstream skill-authoring reference from the agent platform in use, if publicly available (e.g., the platform's canonical skill-creator implementation).`

**3. (MEDIUM) Fix agentskills.io URL issue**
**File:** `skills/skill-writer/SOURCES.md`
- Find the `agentskills.io/specification` row
- Add note in Usage Constraints: `Unverified — confirm URL is live and content matches claimed spec before using in synthesis.`
- Change confidence from any high value to `low`

**File:** `skills/skill-writer/references/claude-code-extensions.md`
- Change the agentskills.io link to a note: `Based on the Agent Skills specification (verify current URL at agentskills.io if needed).`

**4. (MEDIUM) Canonicalize transformed-examples rule**
- The requirement is stated in 4 files: synthesis-path.md, authoring-path.md, mode-selection.md, registration-validation.md
- Keep the full definition ONLY in authoring-path.md
- In the other 3 files, replace with: `Confirm transformed example artifacts exist per authoring-path.md §Example artifact requirements.`

**5. (LOW) Add claude-code-extensions.md to SKILL.md routing table**
- Add row: `| Author Claude Code-specific argument/invocation features | \`references/claude-code-extensions.md\` |`

**6. (LOW) Pin pyyaml in quick_validate.py**
**File:** `skills/skill-writer/scripts/quick_validate.py`
- Change `dependencies = ["pyyaml"]` to `dependencies = ["pyyaml>=6.0,<7"]`

**7. (LOW) Fix provider-specific language in SOURCES.md changelog**
- Change "Codex upstream, Claude upstream" to "upstream platform skill-authoring references"

**8. (LOW) Add output format schema to SKILL.md**
- Expand the Output format section with one-line descriptions per section:
```markdown
1. **Summary** — one paragraph: what was created/changed and the selected skill class
2. **Changes Made** — bulleted list of files, one-line rationale each
3. **Validation Results** — pass/fail/warnings from validator
4. **Open Gaps** — numbered list of unresolved gaps (empty if none)
```

### skill-scanner fixes

**9. (HIGH) Add explicit error recovery**
**File:** `skills/skill-scanner/SKILL.md`
- After Phase 2, add:
```markdown
If `scan_skill.py` returns `{"error": ...}` or exits non-zero, report the error to the user. Ask whether to continue with manual analysis. Do not proceed silently.
```

**10. (MEDIUM) Fix Phase 3 ambiguous skip instruction**
- Replace the Note in Phase 3 with:
```markdown
**Phase 3 scope**: Skip field presence and name-mismatch checks (script handles these). Check only: (1) is `allowed-tools` present and justified? (2) is a model override present and explained? (3) does the description accurately represent the instructions?
```

**11. (MEDIUM) Add worked example to output format**
- After the output template code block, add one concrete filled-in example finding

**12. (MEDIUM) Fix LOW-confidence action**
- Change "Do not report" to: `Do not report as a finding. If relevant to the overall assessment, add one sentence to the Assessment section.`

**13. (LOW) Collapse Phases 5-6 to reduce file length**
- Phases 5 and 6 re-list patterns from reference files. Collapse each to a single load-gate line + scope sentence

**14. (LOW) Add phases 4-6 parallelization note**
- After Phase 3: `Phases 4–6 are independent — complete them in any order.`

### playwright fixes

**15. (HIGH) Fix version check/execution mismatch**
**File:** `skills/playwright/scripts/playwright_cli.sh`
- Find the availability check: `npx --no-install playwright-cli --version`
- Change to match the execution command: `npx --no-install --package @playwright/cli@1.50.1 playwright-cli --version`
- Update the error message to mention the specific version

**16. (MEDIUM) Fix guardrail text to match script behavior**
**File:** `skills/playwright/SKILL.md`
- Find the guardrail about "confirm with the user that auto-installing @playwright/cli via npx is acceptable"
- Replace with: `If \`@playwright/cli@1.50.1\` is not found, the wrapper exits with an install hint. Run \`npm install @playwright/cli@1.50.1\` first.`

**17. (MEDIUM) Add load gate for cli.md**
- Find the reference to cli.md with no gate
- Change to: `Need a full command reference? Load [references/cli.md](references/cli.md).`

**18. (MEDIUM) Remove duplicate security warning in cli.md**
**File:** `skills/playwright/references/cli.md`
- Find the FIRST occurrence of the eval/run-code security warning (the one that appears before eval is even introduced)
- Remove it. Keep only the one in the DevTools section near the actual eval command

**19. (LOW) Add version staleness note**
- In Prerequisites, add: `Version 1.50.1 is pinned for reproducibility. To use a newer release, replace the version in \`scripts/playwright_cli.sh\` and the install commands.`

**20. (LOW) Add failure output format**
- In Output section, add: `On failure, report: the command that failed, the error message verbatim, and the recovery step taken.`

### Verify
```bash
ls skills/skill-writer/references/examples/happy-path-skill.md  # should exist
ls skills/skill-writer/references/examples/antipattern-skill.md  # should exist
grep -n 'Anthropic/Claude' skills/skill-writer/references/synthesis-path.md  # should be zero
grep -n 'confirm with the user that auto-installing' skills/playwright/SKILL.md  # should be zero
grep -n 'npx --no-install playwright-cli' skills/playwright/scripts/playwright_cli.sh  # should have --package flag
pnpm run format -- skills/skill-writer/ skills/skill-scanner/ skills/playwright/
pnpm run format:check
```

**Boundaries:** Only modify skill-writer, skill-scanner, and playwright files.

The staged files are exactly what I should have. The other changes in the "Changes to be staged" section were already staged before I started (pre-existing work on other features). Let me verify the specific files I staged are correct:

**Implementation complete.** All 20 fixes applied across the 3 skills. Here's a summary:

---

### Files Changed

**skill-writer:**
- `references/examples/happy-path-skill.md` — **new**: correct SKILL.md example artifact
- `references/examples/antipattern-skill.md` — **new**: anti-pattern + corrected SKILL.md artifact
- `references/synthesis-path.md` — removed Anthropic/Claude URL (#2), canonicalized transformed-examples rule (#4)
- `SOURCES.md` — agentskills.io confidence→low + unverified note (#3), changelog provider-specific language fixed (#7)
- `references/claude-code-extensions.md` — replaced agentskills.io link with note (#3)
- `references/mode-selection.md` — canonicalized transformed-examples rule to point at authoring-path.md (#4)
- `references/registration-validation.md` — canonicalized transformed-examples rule (#4)
- `SKILL.md` — added claude-code-extensions.md row to routing table (#5), expanded output format schema (#8)
- `scripts/quick_validate.py` — pinned pyyaml to `>=6.0,<7` (#6)

**skill-scanner:**
- `SKILL.md` — error recovery after Phase 2 (#9), Phase 3 scope clarification (#10), worked example finding (#11), LOW action updated (#12), Phases 5–6 collapsed (#13), phases 4–6 parallelization note (#14)

**playwright:**
- `scripts/playwright_cli.sh` — version check now uses `--package @playwright/cli@1.50.1` to match execution (#15)
- `SKILL.md` — guardrail updated to install-hint (#16), cli.md load gate added (#17), version staleness note added (#19), failure output format added (#20)
- `references/cli.md` — removed duplicate security warning from Core section (#18)

**Verification:** All 5 grep checks pass, `format:check` clean.
