## builder: ## Fix all review issues in render-deploy skill

**Working directory:** /Users/josorio/Code/pi-skills

Read each file before editing. Fix these issues:

### 1. `skills/render-deploy/SKILL.md`

**Issue A — Method 2 Prerequisites note (~line 246):** Replace the note that only mentions Blueprint/Dashboard with one that also includes the REST API:
Find: `**Note:** The CLI cannot create PostgreSQL databases or Key-Value (Redis) stores. Use the Blueprint method or the Render Dashboard for those resources.`
Replace: `> **Note:** The CLI cannot create PostgreSQL databases or Key-Value (Redis) stores. Use the Blueprint method or the REST API — see [references/render-api.md](references/render-api.md) for `curl` commands.`

**Issue B — Step 7 metrics reference uses backticks instead of Markdown link:**
Find the line that references `render-api.md` with backtick path format and change to a proper Markdown link: `[references/render-api.md](references/render-api.md)`

**Issue C — `render-debug` skill reference at end of file:** This skill doesn't exist. Remove the entire paragraph about `render-debug` (including the `Optional:` line and trailing content after the `---` separator). Or replace with a generic note like: "For deeper diagnostics, check the Render Dashboard logs and metrics."

**Issue D — Step 2.5 heading:** Renumber "Step 2.5" to "Step 3" and bump all subsequent steps (current Step 3 → Step 4, etc. through Step 7 → Step 8). Actually — read the full section to understand the numbering before renumbering. Make sure it's clean 1-through-N.

**Issue E — Inconsistent note formatting:** The Method 2 section uses bare `**Note:**` while other sections use `> **Note:**` blockquotes. Convert all bare `**Note:**` in Method 2 to blockquote style `> **Note:**` for consistency.

**Issue F — `error-patterns.md` is never referenced in SKILL.md:** Add a reference in the post-deploy/troubleshooting section where it makes sense (near where `troubleshooting-basics.md` is referenced).

### 2. `skills/render-deploy/references/direct-creation.md`

**Issue A — Wrong path prefix (3 instances):** `references/render-api.md` should be `render-api.md` since the files are siblings in the same directory. Also convert from backtick inline code to proper Markdown links. Find all 3 instances:
- `see \`references/render-api.md\` for the exact \`curl\` command.` → `see [render-api.md](render-api.md) for the exact `curl` command.`
- Same pattern for all 3 occurrences (lines ~66, ~70, ~120)

**Issue B — Step 3 Dashboard note (line ~97):** 
Find: `**Note:** For database connection strings, copy the internal URL from the Render Dashboard service detail page.`
Replace: `> **Note:** For database connection strings, use the REST API to retrieve them programmatically — see [render-api.md](render-api.md). Use `internalConnectionString` for services on Render; `externalConnectionString` for external access.`

### 3. `skills/render-deploy/references/deployment-details.md`

**Issue A — Dashboard-only note for databases (lines ~22-23):**
Find: `**PostgreSQL databases and Key-Value stores:** View and manage these on the Render Dashboard — the CLI does not support listing or managing databases and Key-Value stores directly.`
Replace: `**PostgreSQL databases and Key-Value stores:** The CLI does not support database or Key-Value operations. Use the REST API — see [render-api.md](render-api.md) for `curl` commands.`

Remove the Dashboard URL code block that follows it if there is one.

**Issue B — Quick Reference note (~line 168):**
Find: `**Note:** Databases (Postgres, Redis/Key-Value) and metrics are not available via the CLI. Use the Blueprint method or the Render Dashboard for those resources.`
Replace: `**Note:** Databases (Postgres, Redis/Key-Value) and metrics are not available via the CLI. Use the REST API for these — see [render-api.md](render-api.md).`

**Issue C — Wrong troubleshooting link (~line 223):**
Find any reference linking to `configuration-guide.md` for troubleshooting and change to `troubleshooting-basics.md`.

### 4. `skills/render-deploy/references/blueprint-spec.md`

**Issue A — Typo in CLI command (~line 728):**
Find: `render blueprint validate` (singular, missing the 's')
Replace: `render blueprints validate`
Make sure not to change other correct instances of `render blueprints validate`.

### 5. `skills/render-deploy/references/render-api.md`

**Issue A — Missing ownerId note for Key-Value section:**
After the Key-Value creation curl example, add a note:
`The \`ownerId\` is the same workspace ID used for PostgreSQL creation above — retrieve it with \`GET /owners\`.`

### 6. Final verification

```bash
# No MCP references
grep -ri "mcp" skills/render-deploy/ --include="*.md"

# No wrong path prefixes in references/ files
grep -rn "references/render-api" skills/render-deploy/references/ --include="*.md"

# No stale Dashboard-only fallbacks (check manually)
grep -rn "Dashboard" skills/render-deploy/ --include="*.md"

# Blueprint typo fixed
grep -rn "render blueprint " skills/render-deploy/ --include="*.md"

# Format check
pnpm run format:check
```

**Boundaries:** Only modify render-deploy skill files.

All checks pass. Here's a summary of what was done:

---

### Changes made

**`skills/render-deploy/SKILL.md`**
- **Issue D** — Renumbered steps: `Step 2.5 → 3`, `3 → 4`, `4 → 5`, `5 → 6`, `6 → 7`, `7 → 8` (clean 1–8 sequence)
- **Issue E** — Converted bare `**Note:**` in Method 2 Prerequisites (image-backed note) to `> **Note:**` blockquote
- **Issue A** — Replaced PostgreSQL/Key-Value note with blockquote `> **Note:**` referencing REST API with Markdown link
- **Issue B** — Fixed Step 8 metrics reference from backtick path to Markdown link `[references/render-api.md](references/render-api.md)`
- **Issue C** — Removed `render-debug` skill paragraph; replaced with: "For deeper diagnostics, check the Render Dashboard logs and metrics."
- **Issue F** — Added `[references/error-patterns.md](references/error-patterns.md)` reference in the post-deploy troubleshooting section

**`skills/render-deploy/references/direct-creation.md`**
- **Issue A** — Fixed 3 instances: backtick `references/render-api.md` → Markdown link `[render-api.md](render-api.md)` (PostgreSQL note, Key-Value note, health metrics note)
- **Issue B** — Replaced Dashboard-copy note with blockquote directing to REST API with `internalConnectionString`/`externalConnectionString` guidance

**`skills/render-deploy/references/deployment-details.md`**
- **Issue A** — Replaced Dashboard-only note + removed Dashboard URL code block; now references REST API
- **Issue B** — Updated Quick Reference note to reference REST API instead of Dashboard
- **Issue C** — Fixed troubleshooting link from `configuration-guide.md` → `troubleshooting-basics.md`; also removed "debug skill" mention

**`skills/render-deploy/references/blueprint-spec.md`**
- **Issue A** — Fixed typo: `render blueprint validate` → `render blueprints validate`

**`skills/render-deploy/references/render-api.md`**
- **Issue A** — Added `ownerId` note after Key-Value creation curl block

### Verification results
- ✅ No MCP references
- ✅ No `references/render-api` path prefix in references/ files  
- ✅ No `render blueprint ` (singular) anywhere
- ✅ No `render-debug` in SKILL.md (one remains in `troubleshooting-basics.md` — not listed in scope)
- ✅ No bare `**Note:**` in Method 2
- ✅ `pnpm run format:check` — all files pass Prettier
