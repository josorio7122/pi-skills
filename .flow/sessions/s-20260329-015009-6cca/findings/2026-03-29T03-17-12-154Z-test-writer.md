## test-writer: ## Task 1a: Write tests for dead export removal in posthog-skill

**Working directory:** /Users/josorio/Code/pi-skills

**Context:** We need to remove dead exports from posthog-skill. Write tests that verify these exports are gone.

**What to do:**
1. Read these files first to understand current exports:
   - skills/posthog-skill/scripts/lib/dashboard-spec.ts (exports `DashboardSpec` interface — should be removed)
   - skills/posthog-skill/scripts/lib/posthog-client.ts (exports `patchDashboard` on the client — should be removed)
   - skills/posthog-skill/scripts/lib/fixtures.ts (exports `FixtureKey` type that includes `'status'` and `'inspect-live'` — those keys should be removed)

2. Create `skills/posthog-skill/scripts/__tests__/dead-exports.test.ts` with tests:
   - test that `DashboardSpec` is NOT exported from dashboard-spec.ts (import `* as spec` and check `'DashboardSpec' in spec` is false — but since it's a type, this won't work at runtime. Instead: verify by reading the source file and checking the string `export interface DashboardSpec` does not appear)
   - test that `patchDashboard` is NOT on the client object (create a client with `createClient({ projectId: 'test', host: 'http://localhost', personalApiKey: 'test' })` and assert `!('patchDashboard' in client)`)
   - test that `getFixture('status')` throws or returns undefined (currently it returns a fixture — after removal it should throw)
   - test that `getFixture('inspect-live')` throws or returns undefined

3. Run the tests — they should ALL FAIL since the dead exports still exist. Show the failure output.

**Test framework:** Use `node:test` (describe/it) and `node:assert/strict`, matching existing test patterns.
**File:** skills/posthog-skill/scripts/__tests__/dead-exports.test.ts

---

**Test file:** `skills/posthog-skill/scripts/__tests__/dead-exports.test.ts`

**4 new tests written:**

| Test | Why it fails (RED) |
|---|---|
| `dashboard-spec.ts does not export DashboardSpec interface` | Source file still contains `export interface DashboardSpec` |
| `client object does not expose patchDashboard` | `createClient()` return object still includes `patchDashboard` |
| `getFixture("status") throws` | `'status'` key is still in `FIXTURES` — no exception thrown |
| `getFixture("inspect-live") throws` | `'inspect-live'` key is still in `FIXTURES` — no exception thrown |

**RED proof:**
```
✖ dashboard-spec.ts does not export DashboardSpec interface (1.12ms)
  AssertionError: DashboardSpec interface must not be exported from dashboard-spec.ts

✖ client object does not expose patchDashboard (0.20ms)
  AssertionError: patchDashboard must not be a property of the PostHogClient object

✖ getFixture("status") throws (0.18ms)
  AssertionError: Missing expected exception: getFixture("status") must throw — "status" fixture key should be removed

✖ getFixture("inspect-live") throws (0.08ms)
  AssertionError: Missing expected exception: getFixture("inspect-live") must throw — "inspect-live" fixture key should be removed

4 failed, 108 passed
```

**Staged:** `skills/posthog-skill/scripts/__tests__/dead-exports.test.ts`
