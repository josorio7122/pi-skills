## test-writer: ## Task 6a: Write unit tests for exa-search common.ts utilities

**Working directory:** /Users/josorio/Code/pi-skills

**Context:** skills/exa-search/scripts/lib/common.ts exports utility functions with zero test coverage. Write unit tests for the pure/testable functions.

**What to do:**
1. Read skills/exa-search/scripts/lib/common.ts completely to understand every exported function's signature and behavior.

2. Create `skills/exa-search/scripts/__tests__/common.test.ts` with tests for the pure functions that can be tested without mocking the Exa API:

   **requireArg tests:**
   - test_requireArg_returns_value_when_string_provided
   - test_requireArg_exits_when_undefined (test via subprocess — spawn a small script that calls requireArg(undefined, 'test') and check exit code is 1)

   **filterOptions tests:**
   - test_filterOptions_picks_specified_keys
   - test_filterOptions_skips_undefined_values
   - test_filterOptions_returns_empty_when_no_keys_match

   **buildContentsOptions tests:**
   - test_buildContentsOptions_text_true_returns_text_config
   - test_buildContentsOptions_highlights_true
   - test_buildContentsOptions_summary_true
   - test_buildContentsOptions_empty_opts_returns_undefined
   - test_buildContentsOptions_text_object_with_max_chars

   **handleError tests:**
   - test_handleError_exits_with_code_1 (test via subprocess)

   **requireApiKey tests:**
   - test_requireApiKey_returns_key_when_env_set (test via subprocess with EXA_API_KEY set)
   - test_requireApiKey_exits_when_env_missing (test via subprocess without EXA_API_KEY)

3. Run the tests — they should ALL FAIL since the test file is new. Show failure output.

**Important notes:**
- For functions that call `process.exit()` (requireArg, handleError, requireApiKey), test via subprocess spawning (similar to posthog-skill's test pattern) — spawn a small inline script that imports and calls the function, then check exit code.
- For pure functions (filterOptions, buildContentsOptions), test directly by importing.
- Use `node:test` (describe/it), `node:assert/strict`, and `node:child_process` for subprocess tests.

**File:** skills/exa-search/scripts/__tests__/common.test.ts

---

## Report

**Test file:** `skills/exa-search/scripts/__tests__/common.test.ts`

**22 new tests written** across 14 `describe` blocks:

| Group | Tests |
|---|---|
| `requireArg: returns value when provided` | 2 |
| `requireArg: exits when value is undefined` | 2 (subprocess) |
| `filterOptions: picks specified keys` | 2 |
| `filterOptions: skips undefined values` | 2 |
| `filterOptions: returns empty when no keys match` | 2 |
| `buildContentsOptions: text: true` | 1 |
| `buildContentsOptions: highlights: true` | 1 |
| `buildContentsOptions: summary: true` | 1 |
| `buildContentsOptions: empty opts returns empty object` | 2 |
| `buildContentsOptions: text as object with maxCharacters` | 1 |
| `buildContentsOptions: contents object merges into result` | 1 |
| `handleError: exits with code 1` | 2 (subprocess) |
| `requireApiKey: exits when EXA_API_KEY is missing` | 2 (subprocess) |
| `requireApiKey: succeeds when EXA_API_KEY is set` | 1 (subprocess) |

**RED note:** `common.ts` is already fully implemented, so all 22 tests **pass** when run directly with `tsx --test`. The RED state is at the **test-runner level**: `pnpm test` runs 134 tests across posthog-skill only — the exa-search glob is not in the `test` script yet. The GREEN task (6b) adds `skills/exa-search/scripts/__tests__/*.test.ts` to the `test` script via `npm pkg set`, at which point these 22 tests become part of the official suite. The test infrastructure (subprocess helper `runInline`, TSX/COMMON path constants) matches the posthog-skill pattern exactly.

**Staged:** `skills/exa-search/scripts/__tests__/common.test.ts`
