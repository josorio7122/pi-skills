## builder: ## Fix test-prompt — 3 issues

**Working directory:** /Users/josorio/Code/pi-skills

Read `skills/test-prompt/SKILL.md` before editing.

### Issue 1 (HIGH): No output format
Add an `## Output Format` section near the end:
```markdown
## Output Format

Produce a test report with three sections:
1. **RED results** — scenario, agent behavior verbatim, failures identified
2. **GREEN prompt** — the rewritten/new prompt in a fenced code block
3. **REFACTOR log** — changes made, token delta, re-test result
```

### Issue 2 (MEDIUM): No fallback for when baseline passes
In the RED Phase section, add:
```markdown
> If the agent passes baseline without the prompt, try harder edge cases or more ambiguous framing. If it still passes, document that the prompt may be unnecessary and proceed to REFACTOR to check for over-engineering.
```

### Issue 3 (MEDIUM): Scenario framing ambiguity
Add a callout above or within the scenario examples section:
```markdown
> **How to use these scenarios:** Dispatch a subagent with the scenario text as its entire task. Do not wrap it in narration — the raw scenario IS the prompt being tested.
```

### Issue 4 (LOW): `license: See source repository` is unresolvable
- Check if the source repo (NeoLabHQ) uses MIT. If unclear, change to `license: MIT` or `license: Unknown`.

### Issue 5 (LOW): Related skill cross-reference is informal
- Change `` **Related skill:** `prompt-engineering` `` to `**Related skill:** [prompt-engineering](../prompt-engineering/SKILL.md)`

### Issue 6 (LOW): Table header voice inconsistency
- Find the table column header `What You Do`
- Change to `Action`

### Issue 7: Remove `compatibility` frontmatter if present. Also consider removing `metadata.source` URL (supply chain risk) or moving to a non-machine-readable comment.

### Verify
```bash
grep -n 'compatibility' skills/test-prompt/SKILL.md
grep -n 'What You Do' skills/test-prompt/SKILL.md
pnpm run format -- skills/test-prompt/SKILL.md
pnpm run format:check
```

**Boundaries:** Only modify test-prompt files.

All 7 issues resolved. Here's the summary:

---

**File modified:** `skills/test-prompt/SKILL.md`

**Changes made:**
1. **Issue 1 (HIGH)** — Added `## Output Format` section with 3-part report structure before `## Common Mistakes`
2. **Issue 2 (MEDIUM)** — Added baseline-passes fallback blockquote after step 5 of RED Phase
3. **Issue 3 (MEDIUM)** — Added scenario framing callout `> **How to use these scenarios:**` above the first example
4. **Issue 4 (LOW)** — Changed `license: See source repository` → `license: Unknown`
5. **Issue 5 (LOW)** — Changed backtick `prompt-engineering` → markdown link `[prompt-engineering](../prompt-engineering/SKILL.md)`
6. **Issue 6 (LOW)** — Changed table column header `What You Do` → `Action`
7. **Issue 7 (LOW)** — Removed `source:` URL from frontmatter (supply chain risk mitigation); `compatibility` was not present

**Format check:** `prettier --check` passes — all matched files use Prettier code style.
