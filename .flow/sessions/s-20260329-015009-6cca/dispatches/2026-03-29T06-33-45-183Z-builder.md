---
agent: "builder"
task: "## Remove any remaining forbidden frontmatter from exa-search and gh\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nA previous review scout reported forbidden frontmatter in exa-search and gh. Verify whether this is true by reading the actual current files.\n\n### Instructions\n1. Read `skills/exa-search/SKILL.md` — check frontmatter between `---` markers. If ONLY `name` and `description` are present, do nothing. If `compatibility`, `metadata`, `author`, `version`, or `license` are present, remove them.\n\n2. Read `skills/gh/SKILL.md` — same check.\n\n3. Read `skills/frontend-design/SKILL.md` — same check.\n\n4. Read `skills/docker-model-runner/SKILL.md` — same check.\n\n5. For ALL 15 skills, verify frontmatter is clean:\n```bash\nfor f in skills/*/SKILL.md; do\n  skill=$(dirname \"$f\" | xargs basename)\n  # Extract frontmatter fields (excluding name/description/continuation lines)\n  bad=$(awk '/^---$/{n++; if(n==2) exit} n==1 && !/^(name|description|  |---)/' \"$f\" | grep -vE '^$')\n  if [ -n \"$bad\" ]; then\n    echo \"FAIL $skill: $bad\"\n  else\n    echo \"PASS $skill\"\n  fi\ndone\n```\n\nIf any skill has forbidden fields, remove them.\n\n**Boundaries:** Only modify SKILL.md frontmatter if forbidden fields found."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":4,"output":250,"cacheRead":31780,"cacheWrite":1015,"cost":0.01710225,"contextTokens":16593,"turns":2}
---
