---
agent: "builder"
task: "## Add missing Error Recovery and Output Format sections to 5 skills\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### 1. agents-md — add Error Recovery\n**File:** `skills/agents-md/SKILL.md`\nAdd before `## Output`:\n```markdown\n## Error Recovery\n\n- No lock file found → ask the user which package manager they use.\n- Multiple lock files → use the most recently modified one. State the choice.\n- Generated file exceeds 100 lines → cut lowest-value sections. Prioritize: commands > boundaries > code standards.\n- User declines confirmation → ask what to change. Do not write without approval.\n```\n\n### 2. pdf — add Error Recovery section header\n**File:** `skills/pdf/SKILL.md`\nThe error handling content exists inline. Consolidate into a section. Add after the workflow steps, before the output format:\n```markdown\n## Error Recovery\n\n- `pdftoppm` not found → install Poppler (`brew install poppler` or `sudo apt-get install -y --no-install-recommends poppler-utils`).\n- `pdftoppm` returns no pages → the PDF may be corrupt or empty. Report to user.\n- Rendering impossible and user cannot inspect locally → halt delivery. The core rule (never deliver uninspected) overrides all fallbacks.\n- reportlab font/encoding error → verify strings are UTF-8 and image paths are absolute. See [references/techniques.md](references/techniques.md).\n- `get_fields()` returns None → PDF has no AcroForm fields. Tell the user it cannot be filled programmatically.\n```\n\n### 3. playwright — add Error Recovery\n**File:** `skills/playwright/SKILL.md`\nAdd before `## Output`:\n```markdown\n## Error Recovery\n\n- `ref not found` / stale element ref → run `snapshot` to refresh refs, then retry.\n- `@playwright/cli` not found → run `npm install @playwright/cli@1.50.1`.\n- Blank or crashed page → re-`open` the URL. Try `--headed` for debugging.\n- `npx` not found → Node.js is not installed. Ask the user to install it.\n- Session lost → re-`open` the original URL to start a new session.\n```\n\n### 4. posthog-skill — add Error Recovery + Output Format\n**File:** `skills/posthog-skill/SKILL.md`\nAdd after Recommended Workflow, before the per-command docs:\n```markdown\n## Error Recovery\n\n- Exit code 1 (config error) → check `POSTHOG_PROJECT_ID` and `POSTHOG_PERSONAL_API_KEY` are set.\n- Exit code 2 (usage error) → check command syntax against the Quick Reference table.\n- Exit code 3 (partial failure on `create`) → some tiles failed. Soft-delete the dashboard (`PATCH {\"deleted\": true}`) and re-run.\n- `compare` returns empty → verify `POSTHOG_ACH_INSIGHT_ID` is set and the insight exists.\n- Rate limited (429) → wait 60 seconds and retry.\n\n## Output Format\n\nAll commands emit **JSON to stdout** and diagnostics to stderr. Parse output with `jq` or `JSON.parse()`. Key shapes:\n- `status`: `{ project_id, host, token: \"*** (present)\", ach_insight_id }`\n- `inspect`: `{ events: [{ event, properties, count_30d? }] }`\n- `compare`: `{ id, name, query, tiles_count }`\n- `create`: `{ dashboard_id, dashboard_url, tiles: [...] }`\n- `flags list`: `{ results: [{ id, key, active }] }`\n\nFor detailed output shapes, see [references/api-quirks.md](references/api-quirks.md).\n```\n\n### 5. docker-model-runner — add Output Format\n**File:** `skills/docker-model-runner/SKILL.md`\nAdd before `## Reference`:\n```markdown\n## Output Format\n\nDisplay the model's response content directly — not the raw JSON wrapper. For multi-turn conversations, show each response as plain text. If the user asks for raw API output, show the full JSON in a fenced code block.\n```\n\n### Verify\n```bash\nfor f in skills/*/SKILL.md; do\n  skill=$(dirname \"$f\" | xargs basename)\n  er=$(grep -c 'Error Recovery\\|When Things Fail\\|When Things Go Wrong\\|Troubleshooting' \"$f\")\n  of=$(grep -c '## Output\\|Output Format\\|Output Contract' \"$f\")\n  echo \"$skill: ErrorRecovery=$er OutputFormat=$of\"\ndone\nwc -l skills/*/SKILL.md | sort -n\npnpm run format -- skills/agents-md/ skills/pdf/ skills/playwright/ skills/posthog-skill/ skills/docker-model-runner/\npnpm run format:check\n```\n\n**Boundaries:** Only modify the 5 named skill SKILL.md files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":14,"output":2979,"cacheRead":279595,"cacheWrite":12283,"cost":0.17466674999999998,"contextTokens":28160,"turns":12}
---
