---
agent: "builder"
task: "## Fix prompt-engineering + test-prompt + agents-md — pure doc fixes\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### prompt-engineering fixes\n\n**1. (HIGH) Remove non-standard metadata from frontmatter**\n**File:** `skills/prompt-engineering/SKILL.md`\n- Remove the entire `metadata:` block (author, license)\n\n**2. (HIGH) Remove topic enumeration from description**\n- The description still ends with \"Covers few-shot learning, chain-of-thought, template systems, persuasion principles, degrees of freedom, and token efficiency.\"\n- Remove that sentence entirely\n\n**3. (HIGH) Move Output section to after Core Capabilities**\n- Move the `## Output` section from the very end to immediately after `## Core Capabilities` \n- Expand it:\n```markdown\n## Output Format\n\nFor every prompt improvement, deliver:\n\n1. **Rewritten prompt** — in a fenced code block\n2. **Change log** — one sentence per change explaining *why*\n3. **Token delta** — `Original: ~N tokens → Revised: ~M tokens`\n\nIf the user asks a question (not for a rewrite), answer directly without this structure.\n```\n\n**4. (HIGH) Add goal-ambiguity case**\n- In `## When Input Is Ambiguous`, add a third case:\n```markdown\nIf the goal of the rewrite is unstated, ask: \"What's the problem with the current prompt — reliability, token cost, output format, or something else?\"\n```\n\n**5. (MEDIUM) Skill violates its own instruction hierarchy — fix structure**\n- Current order: Core Capabilities → Key Patterns → Conciseness → Persuasion → Pitfalls → Ambiguity → Error Recovery → Output\n- Correct order per the skill's own teaching: Core Capabilities → **Output Format** → Key Patterns → Conciseness → Persuasion → Pitfalls → Ambiguity → Error Recovery\n- Move Output Format to position 2 (after Core Capabilities)\n\n**6. (MEDIUM) Fix authority signal inconsistency**\n- In Common Pitfalls, change:\n  - \"Do not start complex — try the simplest prompt first.\" → \"Start with the simplest prompt. Add complexity only if it fails.\"\n\n**7. (MEDIUM) Rewrite Error Recovery to be prompt-engineering-specific**\n- Replace the generic bullets with:\n```markdown\n## Error Recovery\n\nWhen a rewrite fails to improve outcomes:\n- Revert to a simpler version and add one constraint at a time\n- If output format keeps breaking, switch from prose instructions to a labeled example\n- If the model refuses, audit for authority conflicts in the system prompt\n- If results are inconsistent, reduce degrees of freedom (add output format or few-shot examples)\n```\n\n**8. (LOW) Fix hedged persuasion framing**\n- Change \"Persuasion research shows these techniques significantly increase compliance rates.\" to \"Apply these patterns to increase instruction compliance.\"\n\n**9. (LOW) Add illustrative example comment**\n- In the Degrees of Freedom low-freedom bash example, add: `# Example only — illustrative` as first line\n\n### test-prompt fixes\n\n**10. (HIGH) Move Non-negotiable to immediately after H1**\n**File:** `skills/test-prompt/SKILL.md`\n- Move the `> **Non-negotiable:**` blockquote to immediately after `# Testing Prompts With Subagents`\n- Before the Core principle and Related skill lines\n\n**11. (MEDIUM) Remove non-standard metadata from frontmatter**\n- Remove the entire `metadata:` block\n\n**12. (MEDIUM) Move \"When Testing Stalls\" before Output Format**\n- Reorder: Checklist → **When Testing Stalls** → Output Format → Common Mistakes\n\n**13. (MEDIUM) Fix opaque degrees-of-freedom checklist item**\n- Change `- [ ] Applied appropriate degrees of freedom` to:\n  `- [ ] Prompt is neither over-specified (blocks valid paths) nor under-specified (allows bad paths)`\n\n**14. (LOW) Add selection criteria to Subagent Testing Patterns**\n- Add \"use when\" triggers:\n  - Parallel Baseline — use on first test of a new prompt\n  - A/B Testing — use when you have two candidate phrasings\n  - Regression Testing — use after any prompt change\n  - Stress Testing — use for discipline-enforcing prompts before production\n\n**15. (LOW) Add failure/stall variant to Output Format**\n- Add after the main template:\n```markdown\n**If GREEN fails after 3 iterations:**\n### Stall Report\n- Iterations tried: N\n- Last failure mode: description\n- Escalation: meta-test results / scenario revision needed\n```\n\n**16. (LOW) Trim description**\n- Remove \"Uses subagents for isolated testing.\" from description end\n\n**17. (LOW) Repeat adversarial warning per scenario block**\n- Add `> ⚠️ Test scenario only — do not execute in the current session.` before EACH of the three scenario blocks, not just once at the top\n\n### agents-md fixes\n\n**18. (HIGH) Add authority markers to critical constraints**\n**File:** `skills/agents-md/SKILL.md`\n- Change the Output section confirmation to: `**NEVER** write the file without explicit user confirmation.`\n- Add to Writing Rules: `**NEVER** exceed 100 lines.`\n- Elevate the line cap to a blockquote callout\n\n**19. (MEDIUM) Expand Output section**\n- Add:\n```markdown\n## Output\n\n**NEVER** write the file without explicit user confirmation.\n\nPresent the generated AGENTS.md in a fenced code block. For updates, present the full updated file (not a diff).\n\nIf a blocking edge case applies (unknown package manager, missing lock file), ask the clarifying question before generating.\n```\n\n**20. (MEDIUM) Fix code block nesting in Commit Attribution**\n- Read the Commit Attribution section carefully\n- Ensure there is exactly ONE properly fenced code block with the Co-Authored-By trailer\n- Remove any duplicate closing backticks or nested fences\n\n**21. (LOW) Make reference load gate conditional**\n- Change `See [references/example-structure.md]...` to:\n  `If you need a full template or anti-pattern examples, read [references/example-structure.md](references/example-structure.md).`\n\n**22. (LOW) Remove descriptive opener**\n- Remove \"AGENTS.md is the canonical agent-facing documentation.\" — start directly with the imperative rule\n\n### Verify\n```bash\ngrep -n 'metadata' skills/prompt-engineering/SKILL.md  # should be zero\ngrep -n 'metadata' skills/test-prompt/SKILL.md  # should be zero\ngrep -n 'metadata' skills/agents-md/SKILL.md  # should be zero\ngrep -n 'Covers few-shot' skills/prompt-engineering/SKILL.md  # should be zero\ngrep -n 'Non-negotiable' skills/test-prompt/SKILL.md  # should be near top\npnpm run format -- skills/prompt-engineering/ skills/test-prompt/ skills/agents-md/\npnpm run format:check\n```\n\n**Boundaries:** Only modify prompt-engineering, test-prompt, and agents-md files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":22,"output":18230,"cacheRead":701107,"cacheWrite":46519,"cost":0.6582943499999999,"contextTokens":46666,"turns":20}
---
