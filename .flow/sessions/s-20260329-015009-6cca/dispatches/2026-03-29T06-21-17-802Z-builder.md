---
agent: "builder"
task: "## Fix render-deploy — security patterns + duplication + orphan file\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### 1. Fix `rejectUnauthorized: false` — SECURITY\n**File:** `skills/render-deploy/references/configuration-guide.md`\n- Find the line with `rejectUnauthorized: false`\n- Replace the ENTIRE ssl line with:\n```javascript\nssl: process.env.NODE_ENV === 'production' ? true : false,\n// Render managed Postgres supports verified TLS. Use ssl: true.\n// Only set rejectUnauthorized: false for self-signed certs after understanding the MITM risk.\n```\n\n### 2. Fix `ALLOWED_HOSTS = ['*']` — SECURITY\n**File:** `skills/render-deploy/references/configuration-guide.md`\n- Replace `ALLOWED_HOSTS = ['*']  # ⚠️ Do not use...` with:\n```python\nimport os\nALLOWED_HOSTS = [os.environ.get('RENDER_EXTERNAL_HOSTNAME', 'localhost')]\n```\n\n### 3. Remove `curl | sh` pattern\n**File:** `skills/render-deploy/references/blueprint-workflow.md`\n- Find the line: `- Linux/macOS: \\`curl -fsSL https://raw.githubusercontent.com/render-oss/cli/main/bin/install.sh | sh\\``\n- Replace with: `- Linux/macOS: See https://render.com/docs/cli for official installation instructions.`\n- Keep the `brew install render` line and the warning block above (just remove the curl line)\n\n### 4. Delete orphan file `deployment-details.md`\n- Delete `skills/render-deploy/references/deployment-details.md` — it is never referenced from any file and all its content exists in other references\n\n### 5. Deduplicate runtimes: remove from blueprint-spec.md\n**File:** `skills/render-deploy/references/blueprint-spec.md`\n- Find the entire `## Runtimes` section (which lists Node.js, Python, Go, Ruby, Rust, Elixir, Docker, Image with version details)\n- Replace with a single line: `## Runtimes\\n\\nFor runtime options, supported versions, and build details, see [runtimes.md](runtimes.md).`\n\n### 6. Deduplicate service types: collapse service-types.md\n**File:** `skills/render-deploy/references/service-types.md`\n- This file is 462 lines with YAML examples that duplicate blueprint-spec.md\n- Keep ONLY:\n  - The comparison table at the end (the one comparing all service types side by side)\n  - The \"Best Practices\" bullets for each type (unique content)\n  - The \"When to Use\" guidance for each type (unique content)\n- Remove ALL YAML code blocks (they exist in blueprint-spec.md)\n- Remove ALL \"Required Fields\" and \"Optional Fields\" tables (they exist in blueprint-spec.md)\n- Add at top: `For YAML field definitions and examples, see [blueprint-spec.md](blueprint-spec.md).`\n- Target: <100 lines\n\n### 7. Fix SKILL.md description\n- Remove `by analyzing codebases, generating render.yaml Blueprints, and providing Dashboard deeplinks` from description\n- Keep: `Deploy applications to Render. Use when the user wants to deploy, host, publish, or set up their application on Render's cloud platform.`\n\n### 8. Remove inline troubleshooting from SKILL.md\n**File:** `skills/render-deploy/SKILL.md`\n- Find the `## Troubleshooting` section with the Network Access paragraph\n- Remove the inline paragraph. Keep only the load gate link that already points to troubleshooting-basics.md\n\n### 9. Add authority opener\n- Replace the current opening line (`Render supports **Git-backed** services...`) with:\n```markdown\nYou are the Render deployment agent. Analyze the codebase, select the right service type, and produce a working deployment.\n```\n\n### 10. Remove `rejectUnauthorized: false` mention in service-types.md if present\nCheck `skills/render-deploy/references/service-types.md` for any similar unsafe patterns.\n\n### 11. Fix \"No authentication needed\" in service-types.md\n- Find `**No authentication needed**: Already isolated to account`\n- Replace with: `**Add authentication for sensitive operations** — network isolation alone is not a security boundary.`\n\n### Verify\n```bash\ngrep -rn 'rejectUnauthorized.*false' skills/render-deploy/ --include='*.md' --include='*.yaml'  # should be zero in code examples\ngrep -rn 'ALLOWED_HOSTS.*\\*' skills/render-deploy/ --include='*.md'  # should be zero\ngrep -n 'curl.*|.*sh' skills/render-deploy/references/blueprint-workflow.md  # should be zero\nls skills/render-deploy/references/deployment-details.md 2>/dev/null && echo 'STILL EXISTS' || echo 'DELETED'\nwc -l skills/render-deploy/references/service-types.md  # target <100\nwc -l skills/render-deploy/references/blueprint-spec.md  # should be shorter\npnpm run format -- skills/render-deploy/\npnpm run format:check\n```\n\n**Boundaries:** Only modify render-deploy files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":15,"output":8384,"cacheRead":432850,"cacheWrite":43814,"cost":0.41996250000000007,"contextTokens":44127,"turns":13}
---
