---
agent: "builder"
task: "## Fix posthog-skill — hardcoded IDs + false offline claims + SKILL.md bloat\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing. Run `pnpm test` at the end.\n\n### 1. Remove real project ID `39507` from fixtures\n**File:** `skills/posthog-skill/scripts/lib/fixtures.ts`\n- Line 141: Replace `project/39507/dashboard/99901` with `project/EXAMPLE_PROJECT/dashboard/99901`\n- Line 145: Replace `project/39507/insights/` with `project/EXAMPLE_PROJECT/insights/`\n\n### 2. Remove real insight ID `drOq2lO5` from fixtures\n**File:** `skills/posthog-skill/scripts/lib/fixtures.ts`\n- Line 33: Replace `id: 'drOq2lO5'` with `id: 'dry-run-fixture-id'`\n\n### 3. Fix tests that assert against hardcoded IDs\n**File:** `skills/posthog-skill/scripts/__tests__/create.test.ts`\n- Lines 74-78: The live test asserts `project/39507/dashboard/`. Change to use env var:\n```typescript\nconst projectId = process.env['POSTHOG_PROJECT_ID'] ?? 'EXAMPLE_PROJECT'\n```\nThen assert `parsed.dashboard_url.includes(`project/${projectId}/dashboard/`)`\n- BUT for the dry-run test (which uses fixtures), assert against `EXAMPLE_PROJECT` since that's what fixtures now return\n\n**File:** `skills/posthog-skill/scripts/__tests__/compare.test.ts`\n- Lines 53-56: Change `drOq2lO5` assertion to `dry-run-fixture-id` (matches updated fixture)\n- Lines 72-75: Same — change `drOq2lO5` to `dry-run-fixture-id`\n\n**File:** `skills/posthog-skill/scripts/__tests__/posthog-client.test.ts`\n- Line 8: Change `projectId: '39507'` to `projectId: 'test-project-123'`\n- Update ALL URL assertions in this file that contain `39507` to use `test-project-123`\n- Line 246/249: Change `drOq2lO5` to `test-insight-id`\n\n**File:** `skills/posthog-skill/scripts/__tests__/status.test.ts`\n- Line 37: Change `POSTHOG_PROJECT_ID: '39507'` to `POSTHOG_PROJECT_ID: 'test-project-123'`\n\n### 4. Fix false offline claims in SKILL.md\n**File:** `skills/posthog-skill/SKILL.md`\n- Line 26: Change `(offline)` to remove it or say `(local spec only)`\n- Line 95 (`status`): Remove \"Never requires a token. Safe in CI.\" — it requires POSTHOG_PROJECT_ID. Replace with: \"Requires `POSTHOG_PROJECT_ID`. Token is optional (only needed for `--live`).\"\n- Line 116 (`inspect`): Remove \"Works offline with no env vars.\" Replace with: \"Requires `POSTHOG_PROJECT_ID`. The `--live` flag additionally requires `POSTHOG_PERSONAL_API_KEY`.\"\n\n### 5. Trim SKILL.md from 379 to <150 lines\nRemove these sections entirely from SKILL.md (they duplicate TypeScript source or add no routing value):\n- `## First Use (No Token Yet)` (~lines 318-322) — duplicate of Requirements + Recommended Workflow\n- `## Dashboard Spec` (~lines 324-340) — duplicates `scripts/lib/dashboard-spec.ts`\n- `## Branch Events Catalog` (~lines 341-362) — duplicates `scripts/lib/dashboard-spec.ts`\n- `## File Structure` (~lines 363-379) — adds no routing value\n- The duplicate requirements paragraph (the one after `---` that starts with \"This skill is written in TypeScript...\")\n\nReplace Dashboard Spec + Branch Events Catalog with one line:\n`Branch events and tile definitions are defined in \\`scripts/lib/dashboard-spec.ts\\`. Read that file if you need property lists or query shapes.`\n\nAlso move the detailed per-command documentation sections (the ones under each ### command heading that have example output blocks) to `references/api-quirks.md` (append to existing file). Keep only 1-2 line descriptions per command in SKILL.md.\n\n### 6. Add authority opener\nReplace the `**IMPORTANT — Path Resolution:**` block at the top with:\n```markdown\nYou are the PostHog automation agent for this project. All PostHog reads and writes go through the commands below.\n\n> Path resolution: this is a project-local skill. Resolve script paths relative to the directory containing this SKILL.md.\n```\n\n### 7. Move Write Safety Rule before Command Quick Reference\nCut the `## Write Safety Rule` section and paste it immediately after the authority opener, before the `## Command Quick Reference` table.\n\n### Verify\n```bash\npnpm test 2>&1 | grep -E '^ℹ (tests|pass|fail)'\ngrep -rn '39507' skills/posthog-skill/ --include='*.ts' --include='*.md'\ngrep -rn 'drOq2lO5' skills/posthog-skill/ --include='*.ts' --include='*.md'\nwc -l skills/posthog-skill/SKILL.md  # target <150\n```\n\n**Boundaries:** Only modify posthog-skill files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":18,"output":12304,"cacheRead":556683,"cacheWrite":54638,"cost":0.5565114,"contextTokens":45744,"turns":16}
---
