---
agent: "builder"
task: "## Fix exa-search — 2 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead `skills/exa-search/SKILL.md` before editing.\n\n### Issue 1 (MEDIUM): No error recovery / fallback instructions\nAdd a section after the Rules section (or before it):\n```markdown\n## When Things Go Wrong\n\n- **400 error with `company`/`people` category** — remove domain/date filters (these categories don't support them)\n- **Empty results** — broaden the query, or try `answer.ts` instead\n- **`research.ts` timeout** — use `create` + `poll` separately; the default timeout is 5 minutes\n- **`EXA_API_KEY` not set** — export it: `export EXA_API_KEY=your-key`\n```\n\n### Issue 2: Remove non-standard frontmatter fields\n- Remove the `compatibility` field from frontmatter\n- Remove the `metadata` block (author, version) — it's non-standard\n- The description already mentions EXA_API_KEY requirement\n\n### Issue 3 (LOW): Cost table prices time-sensitive\n- Add a note after the cost table: `Prices as of 2025. Verify at https://exa.ai/pricing.`\n\n### Issue 4 (LOW): research.ts JSDoc says `poll` \"Creates and polls\" — incorrect\n**File: `skills/exa-search/scripts/research.ts`**\n- Find the JSDoc line for `poll` that says \"Create and poll\" or similar\n- Change to: `poll — Poll an existing task until finished (blocks until complete)`\n\n### Verify\n```bash\ngrep -n 'compatibility\\|metadata' skills/exa-search/SKILL.md\ngrep -n 'Create and poll' skills/exa-search/scripts/research.ts\npnpm run format -- skills/exa-search/\npnpm run format:check\n```\n\n**Boundaries:** Only modify exa-search files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":11,"output":1885,"cacheRead":189491,"cacheWrite":9047,"cost":0.11908155,"contextTokens":24830,"turns":9}
---
