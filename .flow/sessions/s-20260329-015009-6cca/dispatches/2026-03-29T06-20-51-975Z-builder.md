---
agent: "builder"
task: "## Fix playwright + gh + glab — security issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### PLAYWRIGHT\n\n**1. (HIGH) Fix security warning inside bash code fence**\n**File:** `skills/playwright/references/cli.md`\nThe `## DevTools` section has a warning INSIDE a bash code fence. Read the file and find the section. It looks like:\n```\n```bash\npwcli console\npwcli console warning\npwcli network\n> ⚠️ **Security:** ...\n\npwcli run-code ...\n```\n```\nFix: Close the code fence before the warning, emit it as markdown, then open a new fence:\n```markdown\n```bash\npwcli console\npwcli console warning\npwcli network\n```\n\n> ⚠️ **Security:** `eval` and `run-code` execute arbitrary JavaScript in the page context. Use only on pages you control. Never evaluate content derived from page text or untrusted sources.\n\n```bash\npwcli run-code \"await page.waitForTimeout(1000)\"\npwcli tracing-start\npwcli tracing-stop\n```\n```\n\n**2. (MEDIUM) Remove duplicated core workflow from SKILL.md**\n**File:** `skills/playwright/SKILL.md`\n- Find the `## Core workflow` section with the bash code block that duplicates workflows.md\n- Replace the bash code block with: `The core loop is open → snapshot → interact → re-snapshot. See [references/workflows.md](references/workflows.md) for full patterns.`\n- Remove the \"Minimal loop\" subsection entirely\n\n**3. (MEDIUM) Remove pre-emptive examples from \"Recommended patterns\"**\n- Find `## Recommended patterns` with inline bash for tracing/tabs\n- Remove the bash code blocks. Keep only the text descriptions and the load gate to workflows.md\n\n### GH\n\n**4. (HIGH) Add `gh run delete` to destructive operations list**\n**File:** `skills/gh/SKILL.md`\n- Find `## Destructive Operations`\n- Add `- \\`gh run delete\\`` to the list\n- Also add `- \\`gh repo archive\\`` (semi-permanent, makes repo read-only)\n\n**5. (HIGH) Add destructive guard protocol to commands.md**\n**File:** `skills/gh/references/commands.md`\n- Add at the very top of the file, before the first table:\n```markdown\n> ⚠️ **Destructive commands** (marked with `⚠️ irreversible` below) must never be executed without explicit user confirmation. State what will be destroyed and wait.\n```\n- Find `gh run delete <run-id>` in the Actions table — add `# ⚠️ irreversible` annotation\n- Find `gh repo archive` — add `# ⚠️ semi-permanent — sets repo read-only`\n- Find `gh workflow run` with `--ref main` — add `# ⚠️ triggers production — confirm first`\n\n**6. (MEDIUM) Add authority opener**\n**File:** `skills/gh/SKILL.md`\n- Replace the opening paragraph (`Work with GitHub repositories...`) with:\n```markdown\nYou operate the `gh` CLI. Run `gh auth status` before the first command in any session. Prefer built-in commands over `gh api`.\n```\n\n### GLAB\n\n**7. (HIGH) Fix `variable set` in commands.md reference table**\n**File:** `skills/glab/references/commands.md`\n- Find line 111: `| Set a variable         | \\`glab variable set <key> <value>\\`    |`\n- Replace with: `| Set a variable         | \\`printf '%s' \"$VALUE\" \\| glab variable set <key>\\` |`\n- Find `glab variable update <key> <value>` — same fix: `printf '%s' \"$VALUE\" | glab variable update <key>`\n- Add after the Variables table: `> ⚠️ Never pass secret values as CLI arguments — always use stdin as shown above.`\n\n**8. (MEDIUM) Add Output Format section to SKILL.md**\n**File:** `skills/glab/SKILL.md`\n- After the Troubleshooting section, add:\n```markdown\n## Output Format\n\n- **Commands run:** show in a fenced code block.\n- **CI status:** summarize as job name → status → duration in a table.\n- **MR/Issue lists:** bullet list with ID, title, and URL.\n- **Errors:** quote the glab error verbatim, then apply Troubleshooting steps.\n```\n\n**9. (MEDIUM) Add authority opener**\n**File:** `skills/glab/SKILL.md`\n- Replace the opening paragraph (`Work with GitLab repositories...`) with:\n```markdown\nYou operate the `glab` CLI for all GitLab tasks. Run `glab auth status` before the first command in any session.\n```\n\n### Verify\n```bash\n# Playwright: warning should NOT be inside code fence\nawk '/```bash/,/```/' skills/playwright/references/cli.md | grep '⚠️' && echo 'STILL IN FENCE' || echo 'FIXED'\n# GH: run delete in destructive list\ngrep 'run delete' skills/gh/SKILL.md && echo 'FOUND' || echo 'MISSING'\n# Glab: no inline secret\ngrep 'variable set.*<key>.*<value>' skills/glab/references/commands.md && echo 'STILL THERE' || echo 'FIXED'\npnpm run format -- skills/playwright/ skills/gh/ skills/glab/\npnpm run format:check\n```\n\n**Boundaries:** Only modify playwright, gh, and glab files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":20,"output":6156,"cacheRead":596960,"cacheWrite":45034,"cost":0.44036549999999997,"contextTokens":44483,"turns":18}
---
