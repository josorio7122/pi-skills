---
agent: "builder"
task: "## Fix gh + glab — security guards + structural issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### gh fixes\n\n**1. (HIGH) Fix PAT exposure — remove heredoc pattern**\n**File:** `skills/gh/SKILL.md`\n- Find the `gh auth login --with-token <<< \"<pat>\"` example\n- Replace with:\n```bash\n# Preferred: pipe from env var so the token never appears in history\necho \"$GITHUB_TOKEN\" | gh auth login --with-token\n# Or use the interactive browser flow:\ngh auth login\n```\n\n**2. (HIGH) Add agent-level destructive command guards**\n**File:** `skills/gh/SKILL.md`\n- Add a new section `## Destructive Operations` after the Output Format section:\n```markdown\n## Destructive Operations\n\n**Never execute these commands without explicit user confirmation in the current message:**\n- `gh repo delete`\n- `gh release delete`\n- `gh cache delete --all`\n- `gh issue delete`\n\nIf the user asks to run any of these, state what will be destroyed and wait for confirmation.\n```\n\n**3. (MEDIUM) Add `gh issue delete` warning to commands.md**\n**File:** `skills/gh/references/commands.md`\n- Find `gh issue delete <number>` — add `# ⚠️ irreversible` comment\n\n**4. (MEDIUM) Add workflow run production guard**\n**File:** `skills/gh/SKILL.md`\n- In the Check CI section or near workflow commands, add:\n`Confirm with the user before triggering any workflow on a production branch (main, master, release/*).`\n\n**5. (MEDIUM) Add load gate for commands.md**\n- Find the reference link to commands.md\n- Change to: `If the user needs a command not shown above, read [references/commands.md](references/commands.md) for the full table.`\n\n**6. (MEDIUM) Add auth failure recovery**\n- In \"When Things Fail\" section, add:\n  - `**Expired or missing token** — run \\`gh auth refresh\\` to renew without re-authenticating`\n  - `**SAML/SSO enforcement** — run \\`gh auth refresh --scopes read:org\\` and authorize via org SSO page`\n\n**7. (MEDIUM) Add JSON output reminder in commands.md**\n**File:** `skills/gh/references/commands.md`\n- Find the JSON Output section\n- Add at the top: `> Always summarize JSON output — do not paste raw JSON into the response. Use \\`--jq\\` to extract only the fields you need.`\n\n**8. (LOW) Remove non-standard metadata from frontmatter**\n**File:** `skills/gh/SKILL.md`\n- Remove the `metadata` block if present\n\n**9. (LOW) Add token env var \"never log\" guidance**\n**File:** `skills/gh/references/commands.md`\n- In the Environment Variables section, add: `Never echo, log, or display token values. Use \\`gh auth status\\` for auth debugging.`\n\n**10. (LOW) Fix decision table passive voice**\n- Change `You need to...` column header to `Goal`\n\n**11. (LOW) Soften --fill constraint**\n- Change \"always use it\" to \"Prefer \\`--fill\\` to auto-populate from commits; override with \\`--title\\`/\\`--body\\` if the user provides a specific description.\"\n\n**12. (LOW) Add run-id discovery step**\n- In Check CI section, before `gh run view`, add: `gh run list --limit 5  # Find the run-id for the failing run`\n\n**13. (LOW) Add generic fallback to When Things Fail**\n- Add: `**Unrecognized error** — run \\`gh <command> --help\\` or check https://cli.github.com/manual/`\n\n### glab fixes\n\n**14. (MEDIUM) Fix unsafe variable set example**\n**File:** `skills/glab/SKILL.md`\n- Find the `glab variable set API_KEY \"$MY_SECRET\"` line\n- Replace with the stdin form only:\n```bash\n# Pipe secrets via stdin — never pass secret values as arguments\nprintf '%s' \"$MY_SECRET\" | glab variable set API_KEY\n```\n- Remove the inline form that contradicts the warning\n\n**15. (MEDIUM) Add experimental label to stacked diffs in SKILL.md**\n- Find `### Stacked Diffs` heading in SKILL.md\n- Change to `### Stacked Diffs *(experimental)*`\n- Add version note: `Requires glab v1.45+.`\n\n**16. (LOW) Add destructive command caveat to commands.md**\n**File:** `skills/glab/references/commands.md`\n- Add near top: `> ⚠️ Commands marked with \\* are irreversible. Confirm intent with the user before running.`\n- Add `*` to: `glab mr delete`, `glab issue delete`, `glab repo delete`, `glab ci delete`, `glab release delete`, `glab token revoke`, `glab securefile remove`\n\n**17. (LOW) Add --fill fallback for empty commits**\n- In Create an MR section, after the --fill instruction, add: `If --fill produces a blank title (no commits or WIP message), add \\`--title 'feat: …'\\` to override.`\n\n**18. (LOW) Add --repo row to routing table**\n- Add: `| Work on a different project | Use \\`--repo owner/repo\\` flag with any command |`\n\n### Verify\n```bash\ngrep -n 'with-token.*pat' skills/gh/SKILL.md  # should be zero\ngrep -n 'Destructive' skills/gh/SKILL.md  # should find section\ngrep -n 'variable set.*\\$MY_SECRET\"$' skills/glab/SKILL.md  # should be zero (no inline form)\npnpm run format -- skills/gh/ skills/glab/\npnpm run format:check\n```\n\n**Boundaries:** Only modify gh and glab files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":20,"output":16277,"cacheRead":672792,"cacheWrite":50429,"cost":0.6351613500000002,"contextTokens":50562,"turns":18}
---
