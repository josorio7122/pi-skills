---
agent: "builder"
task: "## Fix gh — 2 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead `skills/gh/SKILL.md` before editing.\n\n### Issue 1 (HIGH): No error recovery\nAdd a `## When Things Fail` section after the Tips section (or replace Tips):\n```markdown\n## When Things Fail\n\n- **`HTTP 403` or rate limiting** — check `gh auth status`; token may be expired or lack required scopes\n- **Merge conflict on `gh pr merge`** — run `gh pr update-branch` to rebase, then retry\n- **`gh pr checks` returns nothing** — CI may not be configured for this repo; proceed with manual review\n- **Permission denied on merge** — you may not be a maintainer; open a review request instead\n- **Wrong repo detected** — use `--repo owner/repo` to specify explicitly\n```\n\n### Issue 2 (LOW): Tips section is redundant\n- Delete the `## Tips` section entirely. All tips are already shown inline in the workflows above.\n- Or collapse to one genuinely additive tip: \"Most commands infer the repo from git remotes — use `--repo` only for cross-repo operations.\"\n\n### Issue 3 (LOW): No output format instruction\n- Add a brief note near the top or in the workflow:\n```markdown\nFor view/list commands, summarize key fields rather than printing raw output. For create/merge commands, confirm the action and provide the resulting URL.\n```\n\n### Issue 4: Remove `compatibility` frontmatter if present\n\n### Issue 5 (LOW): PAT here-string shell history note\n**File: `skills/gh/SKILL.md`**\n- Find `gh auth login --with-token <<< \"<pat>\"`\n- Add inline comment: `# Note: token appears in shell history. Prefer 'gh auth login' (browser) for interactive sessions.`\n\n### Verify\n```bash\ngrep -n 'Tips' skills/gh/SKILL.md\ngrep -n 'compatibility' skills/gh/SKILL.md\npnpm run format -- skills/gh/SKILL.md\npnpm run format:check\n```\n\n**Boundaries:** Only modify gh files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":9,"output":2012,"cacheRead":133697,"cacheWrite":10961,"cost":0.11141985000000001,"contextTokens":26789,"turns":7}
---
