---
agent: "builder"
task: "## Fix docker-model-runner — 8 Medium+ issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead each file before editing.\n\n### Issue 1 (HIGH): Third-party Go package no version pin\n**File: `skills/docker-model-runner/references/docker-model-guide.md`**\n- Find the Go example with `github.com/sashabaranov/go-openai`\n- Add a note above it: `> **Note:** \\`sashabaranov/go-openai\\` is a community library. The official OpenAI Go SDK is \\`github.com/openai/openai-go\\`. Pin to a specific version in production.`\n\n### Issue 2 (HIGH): Output rule buried at end\n**File: `skills/docker-model-runner/SKILL.md`**\n- Move the `## Output` section to immediately after the Workflow section (before Best Practices)\n- Make it a callout: `> **Output rule:** Display the model's response content directly — not the raw JSON wrapper.`\n\n### Issue 3 (HIGH): No error recovery for OOM/port/API errors\n**File: `skills/docker-model-runner/SKILL.md`**\n- Add a `## Troubleshooting` section:\n```markdown\n## Troubleshooting\n\n- **Port conflict (12434 in use):** `docker model stop-runner && docker model start-runner`\n- **OOM / model won't load:** Try a smaller model (`ai/smollm2`); check `docker model df` for disk usage\n- **API 500 errors:** Check `docker model logs` for the root cause\n- **Model not found:** Run `docker model pull <model>` first — the runner does not auto-pull\n```\n\n### Issue 4 (MEDIUM): Prerequisite in description reduces activation clarity\n**File: `skills/docker-model-runner/SKILL.md`**\n- In frontmatter description, find the trailing \"Requires Docker Desktop or Docker Engine with Model Runner enabled.\"\n- Replace with: \"Covers installation, model management, CLI usage, and OpenAI-compatible API integration.\"\n\n### Issue 5 (MEDIUM): metadata.version/author — stale\n- Remove the entire `metadata` block from frontmatter\n\n### Issue 6 (MEDIUM): api_key=\"not-needed\" needs safety comment\n**File: `skills/docker-model-runner/SKILL.md`** and **`references/docker-model-guide.md`**\n- Find all `api_key=\"not-needed\"` / `apiKey: 'not-needed'` / `\"not-needed\"` patterns\n- Add inline comment: `# Local inference only — do NOT use this pattern with cloud APIs`\n\n### Issue 7 (MEDIUM): No authority anchors\n**File: `skills/docker-model-runner/SKILL.md`**\n- Add authority signals to key steps:\n  - After step 1: `Always verify Docker Model Runner is available before proceeding — never assume it is running.`\n  - After step 4: `Always pull the model before running — the runner does not auto-pull.`\n\n### Issue 8 (MEDIUM): API base URL duplicated\n- Define it once at the top of SKILL.md Workflow:\n```markdown\n> **Default API base:** `http://localhost:12434/engines/llama.cpp/v1`\n```\n- Then reference \"the API base above\" in examples. Keep the reference file examples as-is (they're self-contained).\n\n### Issue 9 (MEDIUM): `sudo usermod` missing warning\n**File: `skills/docker-model-runner/references/docker-model-guide.md`**\n- Find `sudo usermod -aG docker $USER`\n- Add: `# Log out and back in for this to take effect. Note: docker group grants root-equivalent access.`\n\n### Verify\n```bash\ngrep -n 'metadata' skills/docker-model-runner/SKILL.md\ngrep -n 'Requires Docker Desktop' skills/docker-model-runner/SKILL.md\npnpm run format -- skills/docker-model-runner/\npnpm run format:check\n```\n\n**Boundaries:** Only modify docker-model-runner files."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":20,"output":8651,"cacheRead":496857,"cacheWrite":36865,"cost":0.4171258499999999,"contextTokens":37433,"turns":18}
---
