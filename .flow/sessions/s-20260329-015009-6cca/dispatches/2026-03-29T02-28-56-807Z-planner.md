---
agent: "planner"
task: "Plan the restructuring of /Users/josorio/Code/pi-skills. Create tasks.md with sequenced task pairs (RED test-writer + GREEN builder).\n\n## Goal\nRestructure a pi skills package: unify configs at root, split each skill's code into src/ (core) and scripts/ (CLI).\n\n## Changes needed\n\n1. **Unify root config**: Update root tsconfig.json to Node16 module settings + strict options. Delete posthog-skill's package.json, tsconfig.json, pnpm-lock.yaml. Move posthog test/typecheck scripts to root package.json via `npm pkg set`.\n\n2. **posthog-skill restructure**: Move `scripts/lib/posthog-client.ts` → `src/client.ts`, `scripts/lib/dashboard-spec.ts` → `src/dashboard-spec.ts`, `scripts/lib/fixtures.ts` → `src/fixtures.ts`. Create `src/index.ts` re-exporting all. Move unit tests from `scripts/__tests__/posthog-client.test.ts` → `__tests__/client.test.ts` and `dashboard-spec.test.ts` → `__tests__/dashboard-spec.test.ts`. Update imports in run.ts, unit tests, and integration tests (fix tsx binary path from `../../node_modules` to `../../../../node_modules`).\n\n3. **exa-search restructure**: Split `scripts/lib/common.ts` into `src/client.ts` (createClient), `src/options.ts` (filterOptions, buildContentsOptions), `src/index.ts` (re-exports), and `scripts/cli.ts` (parseArgs, requireApiKey, showHelp, handleError, requireArg, executeAndPrint). Update 5 CLI scripts to import from `../src/index.js` and `./cli.js`. Delete `scripts/lib/`.\n\n4. **Update SKILL.md files**: Fix setup instructions to say \"npm install at repo root\".\n\n## Constraints\n- This is a RESTRUCTURE — no new business logic. Existing tests must pass after moves.\n- TDD approach: for restructure tasks, the \"test\" is running existing tests and typecheck after the move.\n- Use `npm pkg set` for package.json changes.\n- All imports must use `.js` extensions (Node16 module resolution).\n- Integration tests use spawnSync with tsx binary path — must be updated.\n- No workspaces, no turbo."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":14,"output":2052,"cacheRead":536712,"cacheWrite":83257,"cost":0.84008225,"contextTokens":83350,"turns":12}
---
