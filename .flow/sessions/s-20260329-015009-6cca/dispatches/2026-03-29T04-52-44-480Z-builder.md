---
agent: "builder"
task: "## Fix posthog-skill — 3 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead each file before editing.\n\n### Issue 1 (HIGH): Missing `references/POSTHOG_API.md`\n**Check:** `ls -la skills/posthog-skill/references/` — is POSTHOG_API.md a broken symlink?\n```bash\nls -la skills/posthog-skill/references/\nfile skills/posthog-skill/references/POSTHOG_API.md 2>/dev/null\n```\nIf broken symlink, remove it. Then check if SKILL.md references it.\n- Read SKILL.md and search for `POSTHOG_API.md`\n- If referenced, either: (a) create a minimal reference file with PostHog REST API basics (auth, rate limits, key endpoints), or (b) remove the reference from SKILL.md\n- The simplest fix: remove the broken symlink and remove any references to it in SKILL.md. The skill already has inline API documentation.\n\n### Issue 2 (MEDIUM): Event count discrepancy\n**File: `skills/posthog-skill/SKILL.md`**\n- Read the Branch Events Catalog table — count the rows\n- Read `skills/posthog-skill/scripts/lib/dashboard-spec.ts` — find `branchEvents` array and count entries\n- If `purchase_started` is in SKILL.md but not in code, remove it from the SKILL.md catalog table\n- Update any count references (e.g., \"See all 9 branch events\") to match\n\n### Issue 3 (MEDIUM): First Use section is user onboarding\n**File: `skills/posthog-skill/SKILL.md`**\n- Find the \"First Use (No Token Yet)\" section\n- Replace it with a single line: \"Run `$RUN status` to confirm config. All commands accept `--dry-run` (no token required).\"\n- Move the detailed walkthrough content is unnecessary — the dry-run mode section already covers this.\n\n### Also fix:\n- Remove `source ~/.zshrc &&` from the live test command — replace with explicit env var\n- Change `.pi/skills/posthog-skill/` in file structure to just `posthog-skill/`\n- Remove the hardcoded dashboard URL `https://us.posthog.com/project/39507/dashboard/1353084` or replace with: \"The `create` command output includes `dashboard_url`.\"\n- Collapse the \"Dry-Run Mode\" section that lists all 11 commands into: \"Every command accepts `--dry-run` — no HTTP calls, same JSON shape, canned fixtures. No env vars required.\"\n\n### Verify\n```bash\npnpm run format -- skills/posthog-skill/SKILL.md\npnpm run format:check\n```\n\n**Boundaries:** Only modify posthog-skill files. Do not touch TypeScript code."
feature: "turbo-evaluation"
exitCode: 0
usage: {"input":18,"output":4448,"cacheRead":380091,"cacheWrite":30023,"cost":0.29338754999999994,"contextTokens":29241,"turns":16}
---
