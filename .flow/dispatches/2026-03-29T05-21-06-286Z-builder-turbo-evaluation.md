---
agent: "builder"
task: "## Fix playwright — 9 Medium+ issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead each file before editing.\n\n### Issue 1 (CRITICAL): `npx --yes` silently auto-installs\n**File: `skills/playwright/scripts/playwright_cli.sh`**\n- Find `npx --yes --package @playwright/cli playwright-cli`\n- Replace `--yes` with `--no-install` and add a pre-flight check:\n```bash\n# Check if @playwright/cli is available\nif ! npx --no-install playwright-cli --version >/dev/null 2>&1; then\n  echo \"Error: @playwright/cli is not installed. Run: npm install @playwright/cli\" >&2\n  exit 1\nfi\ncmd=(npx --no-install --package @playwright/cli playwright-cli)\n```\n- This ensures the script never silently installs — the user must install explicitly first.\n\n### Issue 2 (HIGH): No version pin on @playwright/cli\n- In the same script, pin the package: `--package @playwright/cli@1.50.1` (or whatever version is in the current npm registry — use a reasonable recent version)\n- Also update SKILL.md prerequisite fallback: change `npm install -g @playwright/cli@latest` to `npm install -g @playwright/cli@1.50.1`\n\n### Issue 3 (HIGH): eval/run-code in cli.md have no inline warnings\n**File: `skills/playwright/references/cli.md`**\n- Find the `eval` and `run-code` command entries\n- Add a warning callout directly above them:\n```markdown\n> ⚠️ **Security:** `eval` and `run-code` execute arbitrary JavaScript in the page context. Use only on pages you control. Never evaluate content derived from page text or untrusted sources.\n```\n\n### Issue 4 (HIGH): Guardrails buried at bottom\n**File: `skills/playwright/SKILL.md`**\n- Move the entire `## Guardrails` section to immediately AFTER the `## Skill path` section (before Quick start)\n- This ensures safety rules are loaded before any usage examples\n\n### Issue 5 (HIGH): metadata.author personal identity\n**File: `skills/playwright/SKILL.md`**\n- Remove `metadata` block entirely from frontmatter (author + version)\n\n### Issue 6 (MEDIUM): \"In this repo\" in workflows.md\n**File: `skills/playwright/references/workflows.md`**\n- Find any remaining \"In this repo\" or \"this repo\" references\n- Replace with generic guidance\n\n### Issue 7 (MEDIUM): \"Resolve the full path before use\" is vague\n**File: `skills/playwright/SKILL.md`**\n- Find: `Resolve the full path before use.`\n- Remove this sentence. The harness handles path resolution.\n\n### Issue 8 (MEDIUM): password123 in workflows.md\n**File: `skills/playwright/references/workflows.md`**\n- Find `\"password123\"` in fill examples\n- Replace with `\"$PASSWORD\"` or `\"<password>\"`\n\n### Issue 9 (MEDIUM): No output format specified\n**File: `skills/playwright/SKILL.md`**\n- Add after Guardrails:\n```markdown\n## Output\n\nAfter each significant step, report: the command run, a brief summary of the result, and the path to any saved artifact.\n```\n\n### Issue 10 (MEDIUM): Missing reference load gate\n- Find the line referencing workflows.md and add a trigger condition:\n```markdown\nLoad [references/workflows.md](references/workflows.md) only when the task involves forms, multi-step flows, sessions, or debugging.\n```\n\n### Verify\n```bash\ngrep -n 'npx --yes' skills/playwright/scripts/playwright_cli.sh  # should be zero\ngrep -n 'metadata' skills/playwright/SKILL.md  # should be zero\ngrep -n 'password123' skills/playwright/references/workflows.md  # should be zero\ngrep -n 'this repo' skills/playwright/references/workflows.md  # should be zero\npnpm run format -- skills/playwright/\npnpm run format:check\n```\n\n**Boundaries:** Only modify playwright files."
exitCode: 0
usage: {"input":17,"output":8574,"cacheRead":363518,"cacheWrite":33155,"cost":0.36204764999999994,"contextTokens":33521,"turns":15}
---
