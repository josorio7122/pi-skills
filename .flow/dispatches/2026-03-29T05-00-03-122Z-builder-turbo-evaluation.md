---
agent: "builder"
task: "## Fix glab — 3 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead `skills/glab/SKILL.md` before editing.\n\n### Issue 1 (HIGH): No error recovery\nAdd a `## Troubleshooting` section:\n```markdown\n## Troubleshooting\n\n- **Auth failure / `HTTP 401`** — run `glab auth status`; re-authenticate with `glab auth login`\n- **Wrong remote detected** — use `--repo owner/repo` or set `GITLAB_HOST` for self-hosted instances\n- **SSH remote but glab expects HTTPS** — add an HTTPS remote: `git remote set-url origin https://gitlab.com/owner/repo.git`\n- **Pipeline stuck / no CI configured** — verify `.gitlab-ci.yml` exists in the repo\n- **Permission denied on merge** — check MR approval rules; you may need approvals first\n```\n\n### Issue 2 (MEDIUM): curl injection risk\n**File: `skills/glab/SKILL.md`**\n- Find the curl workaround in Known Limitations that uses `$(glab config get token --host gitlab.com)`\n- Add an inline warning: `# ⚠️ Replace <url-encoded-path> with the literal URL-encoded path — never interpolate unsanitized user input.`\n\n### Issue 3 (MEDIUM): `\"secret-value\"` example looks runnable\n- Find `glab variable set API_KEY \"secret-value\"`\n- Change to: `glab variable set API_KEY \"$MY_SECRET\"` with a comment: `# Use a variable or stdin for secrets: echo \"$MY_SECRET\" | glab variable set API_KEY`\n\n### Issue 4: Remove `compatibility` frontmatter if present\n\n### Issue 5 (LOW): `(Experimental)` label in commands.md\n**File: `skills/glab/references/commands.md`**\n- Find `## Stacked Diffs (Experimental)` heading\n- Change to `## Stacked Diffs`\n\n### Issue 6 (LOW): Known limitations lowercase heading\n- Find `## Known limitations`\n- Change to `## Known Limitations`\n\n### Verify\n```bash\ngrep -n 'secret-value' skills/glab/SKILL.md\ngrep -n 'compatibility' skills/glab/SKILL.md\ngrep -n 'Experimental' skills/glab/references/commands.md\npnpm run format -- skills/glab/\npnpm run format:check\n```\n\n**Boundaries:** Only modify glab files."
exitCode: 0
usage: {"input":10,"output":1955,"cacheRead":178346,"cacheWrite":9980,"cost":0.1202838,"contextTokens":25728,"turns":8}
---
