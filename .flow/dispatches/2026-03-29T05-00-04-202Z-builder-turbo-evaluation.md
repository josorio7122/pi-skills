---
agent: "builder"
task: "## Fix pdf — 5 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead `skills/pdf/SKILL.md` before editing.\n\n### Issue 1 (MEDIUM): No output format for read/extract tasks\nAdd a section:\n```markdown\n## Output format for read/extract tasks\n\n- Return extracted text as Markdown, preserving heading hierarchy where detectable.\n- Render tables as Markdown tables.\n- Note page numbers for each section when layout fidelity matters.\n- If the user asks for a summary, extract key content and summarize — do not dump raw text.\n```\n\n### Issue 2 (MEDIUM): `pdfplumber` vs `pypdf` — no decision rule\n- Find the step that mentions `pdfplumber (or pypdf)`\n- Replace with: `Use \\`pdfplumber\\` for text extraction when table structure matters. Use \\`pypdf\\` for simple full-text extraction or when pdfplumber is unavailable.`\n\n### Issue 3 (MEDIUM): \"merge PDFs\" and \"fill PDF form\" in triggers but no workflow\nAdd brief workflow sections:\n```markdown\n## Merge PDFs\n\nUse `pypdf`'s `PdfMerger`:\n\n```python\nfrom pypdf import PdfMerger\nmerger = PdfMerger()\nfor f in files:\n    merger.append(f)\nmerger.write(\"merged.pdf\")\nmerger.close()\n```\n\n## Fill PDF Forms\n\nUse `pypdf`'s form-fill API for AcroForms. Render and inspect the filled PDF before delivery.\n```\n\n### Issue 4 (MEDIUM): \"when working in this repo\" — project-specific\n- Find: `Write final artifacts under \\`output/pdf/\\` when working in this repo.`\n- Replace with: `Write final output files to the project's existing output directory, or \\`output/\\` if none exists.`\n\n### Issue 5 (LOW): Fallback `pip install` violates uv-only rule\n- Find the `python3 -m pip install` fallback\n- Replace with: `If \\`uv\\` is unavailable, install it first (see https://docs.astral.sh/uv/getting-started/installation/) or ask the user to install the packages manually.`\n\n### Issue 6 (LOW): No error recovery for pdftoppm failure\n- Add: `If \\`pdftoppm\\` returns no pages or an error, report the error to the user. Do not attempt delivery of an uninspected PDF.`\n\n### Issue 7 (LOW): Shell variable example with no concrete values\n- Find `pdftoppm -png $INPUT_PDF $OUTPUT_PREFIX`\n- Add a concrete example below it:\n```bash\n# Example:\npdftoppm -png input.pdf output/pages/page\n# Produces: output/pages/page-1.png, output/pages/page-2.png, ...\n```\n\n### Issue 8: Remove `compatibility` frontmatter if present. Also remove tool list from description (\"Uses Poppler for rendering...\") — move to body Prerequisites if not already there.\n\n### Verify\n```bash\ngrep -n 'this repo' skills/pdf/SKILL.md\ngrep -n 'pip install' skills/pdf/SKILL.md\ngrep -n 'compatibility' skills/pdf/SKILL.md\npnpm run format -- skills/pdf/SKILL.md\npnpm run format:check\n```\n\n**Boundaries:** Only modify pdf files."
exitCode: 0
usage: {"input":9,"output":1922,"cacheRead":129076,"cacheWrite":7758,"cost":0.09667229999999999,"contextTokens":22545,"turns":7}
---
