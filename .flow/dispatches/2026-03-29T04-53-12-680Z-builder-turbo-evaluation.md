---
agent: "builder"
task: "## Fix skill-writer — 8 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead each file before editing.\n\n### Issue 1-4 (HIGH): Hardcoded upstream repo paths\n\n**File: `skills/skill-writer/EVAL.md`**\n- Replace ALL `plugins/sentry-skills/skills/skill-writer/` paths with relative paths (e.g., `scripts/quick_validate.py`)\n- Replace `sentry-skills:skill-writer` with just `skill-writer`\n- Replace `pi-agent-integration-eval` with `<target-skill-name>` placeholder\n- Fix the rsync command: add `--exclude='.env' --exclude='.git'` and a security note about /tmp being world-readable\n- Replace `pi --non-interactive -C` with a generic `$AGENT_CMD` variable pattern with examples for multiple agents\n\n**File: `skills/skill-writer/references/synthesis-path.md`**\n- Find the Step 1.2 baseline source pack that references `plugins/sentry-skills/skills/skill-writer/` and `.codex/skills/.system/skill-creator/`\n- Replace with generic language:\n  1. \"This skill's own SKILL.md and references/ (the canonical workflow source)\"\n  2. \"Any local compatibility alias (e.g., skill-creator if present in the repo)\"\n  3. \"The agent's built-in skill-creator if available (system skill)\"\n- Add fallback: \"If a listed source path does not exist in this repository, skip it and proceed with available sources.\"\n\n**File: `skills/skill-writer/SOURCES.md`**\n- Replace `.codex/skills/.system/skill-creator/SKILL.md` with \"Agent built-in skill-creator (system skill, if available)\"\n\n### Issue 5 (HIGH): Ambiguous activation order\n**File: `skills/skill-writer/SKILL.md`**\n- After the routing table and before Step 1, add a disambiguation note:\n```\nFor simple tasks (update wording, fix structure): use the table above to load only the needed references, then jump to the relevant step.\nFor new skill creation or full rewrites: execute Steps 1–7 in sequence.\n```\n\n### Issue 6 (MEDIUM): Routing table duplicates 7-step structure\n- Leave the routing table as-is (it serves as quick lookup). The disambiguation note above resolves the confusion.\n\n### Issue 7 (MEDIUM): \"Primary success condition\" is descriptive\n**File: `skills/skill-writer/SKILL.md`**\n- Find: `Primary success condition: maximize high-value input coverage before authoring so the resulting skill has minimal blind spots.`\n- Replace with: `Maximize high-value input coverage before authoring. Minimize blind spots.`\n\n### Issue 8 (LOW): skill-patterns.md Claude Code section\n**File: `skills/skill-writer/references/skill-patterns.md`**\n- Find the \"Argument-Accepting Skills\" section and add before it: \"Skip this section unless authoring specifically for Claude Code.\"\n\n### Cross-cutting: Remove `compatibility` if present\nCheck if SKILL.md has a `compatibility` frontmatter field. If so, remove it (move content to Prerequisites section if not already there).\n\n### Verify\n```bash\ngrep -rn 'sentry-skills\\|plugins/sentry' skills/skill-writer/\ngrep -rn '\\.codex/' skills/skill-writer/\npnpm run format -- skills/skill-writer/\npnpm run format:check\n```\n\n**Boundaries:** Only modify skill-writer files."
exitCode: 0
usage: {"input":14,"output":6368,"cacheRead":295957,"cacheWrite":33275,"cost":0.30913035,"contextTokens":32968,"turns":12}
---
