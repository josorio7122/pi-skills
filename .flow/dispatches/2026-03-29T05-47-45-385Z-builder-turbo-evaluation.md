---
agent: "builder"
task: "## Fix render-deploy + pdf — structural + content\n\n**Working directory:** /Users/josorio/Code/pi-skills\nRead each file before editing.\n\n### render-deploy fixes\n\n**1. (HIGH) Fix `static` service type shorthand**\n**File:** `skills/render-deploy/SKILL.md`\n- Find the Service Types list with `- \\`static\\`: Static sites`\n- Change to: `- \\`web\\` + \\`runtime: static\\`: Static sites (HTML/CSS/JS served via CDN)`\n\n**2. (HIGH) Trim SKILL.md to router size**\nThe file is 370+ lines. Extract the two full workflows:\n- Move Method 1 steps 1-8 detail content to `references/blueprint-workflow.md` (the steps and inline code blocks)\n- Move Method 2 steps 2-4 detail content into `references/direct-creation.md` (it already exists — merge)\n- Keep in SKILL.md only: frontmatter, intro, Happy Path, Source Path decision, Method Selection heuristic, and load gates to the references\n- Target: <150 lines\n- Replace inline steps with load gates like: `Load [references/blueprint-workflow.md](references/blueprint-workflow.md) and follow it.`\n\n**3. (MEDIUM) Fix description — remove topic enumeration**\n- Remove the trailing clause about databases, cron jobs, and background workers\n- End at: `...on Render's cloud platform.`\n\n**4. (MEDIUM) Add `uv` to runtimes.md Python section**\n**File:** `skills/render-deploy/references/runtimes.md`\n- In Python package managers, add: `- **uv**: Detected if \\`uv.lock\\` exists; \\`uv sync\\` / \\`uv run gunicorn app:app\\``\n\n**5. (MEDIUM) Add ssl warning**\n**File:** `skills/render-deploy/references/configuration-guide.md`\n- Find `ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false }`\n- Add inline comment: `// ⚠️ rejectUnauthorized: false disables TLS verification. For Render-internal connections, use ssl: true instead.`\n\n**6. (MEDIUM) Add ALLOWED_HOSTS warning**\n**File:** `skills/render-deploy/references/configuration-guide.md`\n- Find `ALLOWED_HOSTS = ['*']`\n- Add comment: `# ⚠️ Do not use ['*'] in production. Set to your Render service hostname.`\n\n**7. (MEDIUM) Add render.yaml output format contract**\n**File:** In SKILL.md (after Method Selection)\n- Add: `Output the complete render.yaml as a single fenced YAML code block. Add brief inline comments for fields requiring user action.`\n\n**8. (MEDIUM) Merge Happy Path and Source Path sections**\n- The Happy Path Q1 and the Choose Your Source Path section duplicate the Git vs Docker decision\n- Merge into one flow — keep Happy Path as the master, remove standalone Source Path section\n\n**9. (LOW) Collapse Method 2 content-free stubs**\n- If Method 2 steps are just \"see references/direct-creation.md\", collapse to a single load gate\n\n**10. (LOW) Add migration warning to django template**\n**File:** `skills/render-deploy/assets/python-django.yaml`\n- Find `python manage.py migrate` in buildCommand\n- Add comment: `# ⚠️ Running migrate in buildCommand risks race conditions on multi-instance deploys`\n\n### pdf fixes\n\n**11. (HIGH) Create reference file structure**\nCreate `skills/pdf/references/` with at least one file. Move heavy content from SKILL.md:\n- Create `skills/pdf/references/techniques.md` containing:\n  - The reportlab generation section\n  - The pdfplumber/pypdf extraction section  \n  - The form fill section (with the COMPLETE write example — see issue 12)\n  - The merge section\n- Add load gates in SKILL.md: `For generation, extraction, form filling, or merging, read [references/techniques.md](references/techniques.md).`\n- Keep in SKILL.md: core rule, workflow overview (numbered steps), quality expectations, final checks, dependencies\n\n**12. (HIGH) Fix form fill — add complete write example**\nIn the form fill section (now in references/techniques.md), add after the get_fields() snippet:\n```python\nfrom pypdf import PdfReader, PdfWriter\n\nreader = PdfReader(\"form.pdf\")\nwriter = PdfWriter()\nwriter.clone_reader_document_root(reader)\nwriter.update_page_form_field_values(writer.pages[0], {\"field_name\": \"value\"})\nwith open(\"filled.pdf\", \"wb\") as f:\n    writer.write(f)\n```\n- Add error handling note: `If \\`get_fields()\\` returns None or empty, the PDF has no AcroForm fields and cannot be filled programmatically. Tell the user.`\n\n**13. (MEDIUM) Add security note for untrusted PDFs**\n**File:** `skills/pdf/SKILL.md`\n- Add after core rule: `> ⚠️ Parse PDFs from untrusted sources with caution. Pin \\`pdfplumber\\` and \\`pypdf\\` versions. Never execute embedded JavaScript or follow embedded file paths outside the working directory.`\n\n**14. (MEDIUM) Fix output directory instruction**\n- Change \"project's existing output directory\" to: `Write final output to \\`./output/\\` relative to the current working directory unless the user specifies otherwise.`\n\n**15. (MEDIUM) Add role framing**\n- Add before core rule: `You are a PDF specialist. Your primary obligation is visual correctness.`\n\n**16. (MEDIUM) Consolidate stop condition**\n- Move the rendering fallback (\"If pdftoppm returns no pages...\") to immediately after core rule so all stop-condition logic is in one place\n\n**17. (LOW) Remove duplicate description**\n- Delete the `## When to use` section — it repeats the frontmatter description\n- Remove \"Uses Poppler for rendering, reportlab for generation, pdfplumber and pypdf for extraction.\" from description\n\n**18. (LOW) Add U+2011 rationale**\n- Change the hyphen rule to: `Use ASCII hyphens only. reportlab renders Unicode dashes (U+2011, U+2013, U+2014) as black squares unless a Unicode-capable font is registered.`\n\n**19. (LOW) Delete redundant Prerequisites section**\n- Remove `## Prerequisites` — same info is in Dependencies\n\n**20. (LOW) Add --no-install-recommends to apt-get**\n- Change `sudo apt-get install -y poppler-utils` to `sudo apt-get install -y --no-install-recommends poppler-utils`\n\n### Verify\n```bash\ngrep -n 'static.*Static sites' skills/render-deploy/SKILL.md  # should show web + runtime\nwc -l skills/render-deploy/SKILL.md  # target <150\ngrep -n 'rejectUnauthorized' skills/render-deploy/references/configuration-guide.md  # should have warning\nls skills/pdf/references/  # should exist\ngrep -n 'When to use' skills/pdf/SKILL.md  # should be zero\npnpm run format -- skills/render-deploy/ skills/pdf/\npnpm run format:check\n```\n\n**Boundaries:** Only modify render-deploy and pdf files."
exitCode: 0
usage: {"input":20,"output":13674,"cacheRead":666480,"cacheWrite":50884,"cost":0.5959289999999999,"contextTokens":51318,"turns":18}
---
