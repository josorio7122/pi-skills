---
agent: "builder"
task: "## Slim render-deploy SKILL.md + add pi MCP section + security warning\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nThe render-deploy SKILL.md is 372 lines (longest in repo). Fix multiple issues.\n\n### Step 1: Read all files\n- skills/render-deploy/SKILL.md\n- skills/render-deploy/references/ (list contents)\n\n### Step 2: Add missing frontmatter\n```yaml\nmetadata:\n  author: josorio7122\n  version: '1.0'\ncompatibility: 'Requires Git repository with remote. Optional: Render CLI (render), Render MCP tools.'\n```\n\n### Step 3: Remove empty agents/ directory\n```bash\nrm -rf skills/render-deploy/agents/\n```\n\n### Step 4: Move MCP Setup sections to reference file\nThe MCP Setup sections (### Cursor, ### Claude Code, ### Codex, ### Other Tools — roughly lines 128-200) should be moved to a new file `skills/render-deploy/references/mcp-setup.md`.\n\nIn SKILL.md, replace with a brief note and conditional loading:\n```markdown\nIf MCP tools are not available, see `references/mcp-setup.md` for setup instructions for various environments (pi, Claude Code, Cursor, Codex, and others).\n```\n\n### Step 5: Add pi section to MCP setup\nIn the new `references/mcp-setup.md`, add a `### pi` section FIRST (before Cursor, Claude Code, Codex):\n```markdown\n### pi\n\npi does not currently support MCP servers natively. Use the Render CLI instead:\n\n1. Install: `brew install render` (macOS) or `curl -fsSL https://raw.githubusercontent.com/render-oss/cli/main/bin/install.sh | sh`\n2. Authenticate: `render login` or `export RENDER_API_KEY=\"rnd_xxxxx\"`\n3. Use `render` CLI commands directly via the bash tool.\n```\n\n### Step 6: Add pipe-to-shell security warning\nWherever `curl -fsSL ... | sh` appears (the Render CLI install command), add a warning:\n```markdown\n> ⚠️ **Security:** Piping to shell (`curl | sh`) executes remote code. Review the script at the URL before running, or use `brew install render` instead.\n```\n\n### Step 7: Verify\nTarget: SKILL.md should be ~300 lines or less (down from 372).\nAll content preserved in reference file.\nVerify new reference file exists."
exitCode: 0
usage: {"input":19,"output":6597,"cacheRead":414420,"cacheWrite":17293,"cost":0.28818675000000005,"contextTokens":31222,"turns":17}
---
