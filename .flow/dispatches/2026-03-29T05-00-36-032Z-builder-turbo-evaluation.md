---
agent: "builder"
task: "## Fix agents-md — 4 issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead `skills/agents-md/SKILL.md` before editing.\n\n### Issue 1 (MEDIUM): Provider-specific trigger in description\n- In frontmatter `description`, replace `\"set up CLAUDE.md\"` with `\"set up agent instructions file\"`\n- Remove the trailing sentence `\"Enforces research-backed best practices for agent documentation.\"` — it breaks the `<What>. Use when.` pattern\n\n### Issue 2 (MEDIUM): `ln -s AGENTS.md CLAUDE.md` ties to Claude\n- In the File Setup section, change:\n```\n2. `ln -s AGENTS.md CLAUDE.md`\n```\nTo:\n```\n2. Optionally create provider-specific symlinks (e.g., `ln -s AGENTS.md CLAUDE.md` for Claude-based agents)\n```\n\n### Issue 3 (MEDIUM): No `references/` — 120+ lines in single file\n- Extract the `## Example Structure` block into a new file `skills/agents-md/references/example-structure.md`\n- Replace in SKILL.md with: `See [references/example-structure.md](references/example-structure.md) for a complete template.`\n- Also extract `## Anti-Patterns` into the same reference file or a separate one\n\n### Issue 4 (MEDIUM): No error recovery\n- Add a brief `## Edge Cases` section:\n```markdown\n## Edge Cases\n\n- No lock file found: ask the user which package manager to use\n- Existing AGENTS.md over 100 lines: trim to required sections first, confirm with user\n- Conflicting conventions: defer to the most recently modified config file\n```\n\n### Issue 5 (LOW): Commit Attribution placeholder is vague\n- In the Commit Attribution section, change `(the agent's name and attribution byline)` to a concrete example: `Co-Authored-By: AI Agent <ai@noreply.example.com>`\n\n### Issue 6: Remove `compatibility` frontmatter if present\n\n### Verify\n```bash\ngrep -n 'CLAUDE.md' skills/agents-md/SKILL.md  # should only appear in optional symlink note\nls skills/agents-md/references/\npnpm run format -- skills/agents-md/\npnpm run format:check\n```\n\n**Boundaries:** Only modify agents-md files."
exitCode: 0
usage: {"input":11,"output":7167,"cacheRead":194207,"cacheWrite":12873,"cost":0.21407385,"contextTokens":28804,"turns":9}
---
