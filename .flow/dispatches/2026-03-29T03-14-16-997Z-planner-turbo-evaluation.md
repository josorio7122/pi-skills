---
agent: "planner"
task: "Create a task plan to fix 6 code organization issues in /Users/josorio/Code/pi-skills. All TypeScript code lives under skills/exa-search/scripts/ and skills/posthog-skill/scripts/.\n\n## Issue 1: Extract shared test helper in posthog-skill\nThe `run()` helper function (spawns CLI via `spawnSync(TSX, [RUN, ...args], ...)`) is copy-pasted identically in 6 CLI test files:\n- skills/posthog-skill/scripts/__tests__/status.test.ts\n- skills/posthog-skill/scripts/__tests__/inspect.test.ts\n- skills/posthog-skill/scripts/__tests__/compare.test.ts\n- skills/posthog-skill/scripts/__tests__/create.test.ts\n- skills/posthog-skill/scripts/__tests__/flags.test.ts\n- skills/posthog-skill/scripts/__tests__/live.test.ts (variant adds timeout: 30000)\n\nExtract to `skills/posthog-skill/scripts/__tests__/helpers.ts`. The helper also includes `TSX` and `RUN` path constants that use `path.join(__dirname, '..', '..', '..', '..', 'node_modules', '.bin', 'tsx')` and similar. Also the `LIVE` constant `Boolean(process.env['POSTHOG_TEST_LIVE'])` is duplicated in 5 files.\n\n## Issue 2: Remove dead exports in posthog-skill\n- `DashboardSpec` interface in lib/dashboard-spec.ts — exported, never imported\n- `patchDashboard` method in lib/posthog-client.ts — never called from run.ts\n- `'status'` and `'inspect-live'` fixture keys in lib/fixtures.ts — never requested by run.ts\nNote: `noUnusedLocals` is enabled in tsconfig, so removing unused exports may be required for clean compilation. However, exports themselves don't trigger `noUnusedLocals` — only unused local variables do. Still, dead exports should be removed for hygiene.\n\n## Issue 3: Refactor exa-search scripts to use common.ts consistently\nCurrent state of skills/exa-search/scripts/:\n- lib/common.ts exports 8 utilities: createClient, requireArg, executeAndPrint, showHelp, requireApiKey, parseArgs, handleError, filterOptions, buildContentsOptions\n- answer.ts, search.ts use common.ts helpers well\n- contents.ts manually re-implements parseArgs/showHelp boilerplate instead of using common.ts\n- research.ts manually re-implements parseArgs/showHelp AND has `console.log(JSON.stringify(result, null, 2))` repeated 5× instead of using executeAndPrint\n- search.ts and find-similar.ts share identical wantContents + buildContentsOptions pattern (minor, could extract but may be over-engineering)\n\nFocus: Make contents.ts and research.ts use the common.ts helpers they're ignoring.\n\n## Issue 4: Split run.ts (502 lines) into command modules\nCurrent structure: skills/posthog-skill/scripts/run.ts has ALL commands inline:\n- resolveConfig(), requireToken() — config section\n- out(), info(), handleApiError() — output helpers\n- cmdStatus(config) — sync, small\n- cmdInspect(flags, config) + cmdInspectLive(config) — inspect command\n- cmdCompare(flags, config) + cmdCompareLive(config) — compare command (~80 lines)\n- cmdCreate(flags, config) + cmdCreateLive(config) — create command (~100 lines)\n- parseFlagsOptions(args), cmdFlags(args, config) — flags command (~130 lines)\n- main() — entry point with switch\n\nProposed structure:\n- run.ts — slim entry point (main + switch + imports)\n- lib/config.ts — resolveConfig, requireToken, AppConfig type, out(), info(), handleApiError()\n- lib/cmd-status.ts\n- lib/cmd-inspect.ts\n- lib/cmd-compare.ts\n- lib/cmd-create.ts\n- lib/cmd-flags.ts\n\nEach command file exports a single function. The dry-run pattern (check isDryRun → return fixture → else call live) stays in each command file.\n\n## Issue 5: Type the PostHog insight response in cmdCompareLive\ncmdCompareLive in run.ts has 7+ `Record<string, unknown>` casts to extract fields from the PostHog insight response. Define typed interfaces in posthog-client.ts for the InsightVizNode/query structure and use them.\n\n## Issue 6: Add exa-search tests\n5 scripts + common.ts with zero tests. At minimum test common.ts utilities (createClient, requireArg, parseArgs, filterOptions, buildContentsOptions, handleError). These are pure functions that can be unit tested without mocking the Exa API.\n\n## Constraints\n- Tests run via `pnpm test` which currently only runs posthog tests: `tsx --test skills/posthog-skill/scripts/__tests__/*.test.ts`\n- Need to update root package.json test script to also include exa-search tests (via npm pkg set)\n- All imports use .js extensions (ESM)\n- tsconfig has noUnusedLocals, noUnusedParameters, noUncheckedIndexedAccess enabled\n- Must verify `tsc --noEmit` passes and `pnpm test` passes after each change\n- Must verify `pnpm run lint` passes after each change\n\n## Ordering\nIssues 1-2 are independent quick wins. Issue 3 is independent. Issue 4 depends on issue 5 (type the response interfaces before splitting run.ts, since the types go in posthog-client.ts). Issue 6 is independent but should come last (new tests).\n\nSuggested order: 2 → 1 → 3 → 5 → 4 → 6"
exitCode: 0
usage: {"input":12,"output":6533,"cacheRead":343436,"cacheWrite":58730,"cost":0.7021655,"contextTokens":62365,"turns":10}
---
