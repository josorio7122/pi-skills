---
agent: "builder"
task: "## Fix render-deploy + exa-search + glab + pdf — remaining Medium issues\n\n**Working directory:** /Users/josorio/Code/pi-skills\n\nRead each file before editing.\n\n### render-deploy fixes:\n\n**Issue 1 (MEDIUM): Hardcoded hostnames in asset templates**\n**File: `skills/render-deploy/assets/nextjs-postgres.yaml`**\n- Find `NEXTAUTH_URL` with hardcoded `https://nextjs-app.onrender.com`\n- Change to `sync: false` with comment: `# Set to your Render service URL after deploy`\n\n**File: `skills/render-deploy/assets/python-django.yaml`**\n- Find `DJANGO_ALLOWED_HOSTS` with hardcoded `django-web.onrender.com`\n- Change to `sync: false` with comment: `# Set to your Render service hostname after deploy`\n\n**Issue 2 (MEDIUM): EOL runtime versions**\n**File: `skills/render-deploy/references/runtimes.md`**\n- Find Node.js supported versions — remove `14` and `16`\n- Find Python supported versions — remove `3.8`\n- Find Go supported versions — remove `1.20`\n- Add a note at the top: `> Version lists may be outdated — verify at https://render.com/docs for current supported versions.`\n\n**File: `skills/render-deploy/references/blueprint-spec.md`**\n- Same: remove EOL versions from any version lists (Node 14/16, Python 3.8, Go 1.20)\n\n**Issue 3 (MEDIUM): Content duplication across reference files**\n- In `skills/render-deploy/references/deployment-details.md`, find the env var pattern section and port binding examples\n- Replace with: `For environment variable patterns and port binding examples, see [configuration-guide.md](configuration-guide.md).`\n- Keep the section headings but remove the duplicated content\n\n**Issue 4 (MEDIUM): Happy Path Q2 jargon**\n**File: `skills/render-deploy/SKILL.md`**\n- Find the Happy Path question about \"bring your own infra\"\n- Replace with: `Does your app need a database, background worker, or cache? (yes / no / not sure)`\n\n**Issue 5 (LOW): render-api.md missing credential warning**\n**File: `skills/render-deploy/references/render-api.md`**\n- After the `export RENDER_API_KEY=` line, add: `# Store in ~/.zshrc or .env — never commit to source control`\n\n### exa-search fixes:\n\n**Issue 6 (MEDIUM): \"Prices as of 2025\" time-sensitive**\n**File: `skills/exa-search/SKILL.md`**\n- Find: `Prices as of 2025.`\n- Replace with: `Prices may change.`\n\n**Issue 7 (MEDIUM): Prerequisites repo-specific**\n- Find: `Dependencies are managed at the repository root. Run \\`pnpm install\\` from the repo root if needed.`\n- Replace with: `Run \\`pnpm install\\` from this skill's parent directory if dependencies are not yet installed.`\n\n**Issue 8 (MEDIUM): pollInterval default inconsistency**\n**File: `skills/exa-search/scripts/research.ts`**\n- Find the JSDoc that says `pollInterval` default is 1000\n- Change to 2000 to match the actual code\n\n**Issue 9 (MEDIUM): Decision tree ambiguity for docs**\n**File: `skills/exa-search/SKILL.md`**\n- In the decision tree step 2 (answer.ts), add a parenthetical: `(For comparing multiple doc sources, see Documentation Lookup pattern below.)`\n\n### glab fixes:\n\n**Issue 10 (MEDIUM): Token in CLI flag visible in process list**\n**File: `skills/glab/SKILL.md`**\n- Find the curl workaround with `$(glab config get token)`\n- Add a warning above or inline: `# Token visible in process lists — use only on single-user machines or CI with masked vars`\n\n**Issue 11 (MEDIUM): glab ci run-trig --token unexplained**\n- Find `glab ci run-trig --token <token>`\n- Change to: `glab ci run-trig --token <pipeline-trigger-token>  # Create in Settings → CI/CD → Pipeline triggers`\n\n### pdf fixes:\n\n**Issue 12 (MEDIUM): \"edit\" in description but no handler**\n**File: `skills/pdf/SKILL.md`**\n- Find \"edit\" in the description\n- Remove it. The description should say: `Read, create, and review PDF files...`\n\n**Issue 13 (MEDIUM): No core rule up front**\n- Add at the very top of the body (before ## When to use):\n```markdown\n> **Core rule:** Always render PDFs to PNG and inspect before delivery. Never deliver an uninspected PDF.\n```\n\n**Issue 14 (MEDIUM): Fill PDF Forms section too thin**\n- Find the `## Fill PDF Forms` section\n- Add a field discovery snippet:\n```python\nfrom pypdf import PdfReader\nreader = PdfReader(\"form.pdf\")\nprint(reader.get_fields())  # discover field names\n```\n\n**Issue 15 (MEDIUM): No reportlab error recovery**\n- Add after the workflow step about reportlab:\n```markdown\nIf reportlab raises a font or encoding error, verify all strings are UTF-8 and all image paths are absolute.\n```\n\n### Remove metadata.author/version from all touched skills if still present.\n\n### Verify\n```bash\ngrep -rn 'nextjs-app.onrender' skills/render-deploy/assets/\ngrep -rn 'django-web.onrender' skills/render-deploy/assets/\ngrep -rn '\"14\"\\|\"16\"' skills/render-deploy/references/runtimes.md\ngrep -n 'Prices as of 2025' skills/exa-search/SKILL.md\ngrep -n 'edit' skills/pdf/SKILL.md | head -5\npnpm run format -- skills/render-deploy/ skills/exa-search/ skills/glab/ skills/pdf/\npnpm run format:check\n```\n\n**Boundaries:** Only modify render-deploy, exa-search, glab, and pdf files."
exitCode: 0
usage: {"input":29,"output":7205,"cacheRead":1096699,"cacheWrite":40289,"cost":0.5882554500000001,"contextTokens":52179,"turns":27}
---
